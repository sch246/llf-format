# LLF 字面行格式规范 v0.6 草案

2026-09-21

原名 LTF（Line-Typed Format），v0.4 起改名为 LLF（Literal Line Format，字面行格式）。改名理由见第 12 节。

## 1. 概述与设计目标

LLF 是一种以行为单位的数据格式，主要面向大模型的工具调用，也适合手写。

格式本身**不写类型、不做类型判断**。它存储的东西只有四样：字典（`{}`）、列表（`[]`）、字符串（`-`）、null（`_`）。标量一律是字符串，某段字符串该被读成整数、浮点、布尔、日期还是别的什么，由使用者的 **JSON Schema** 决定。格式里没有任何类型标记，也没有类型逃生口。

设计目标：

- **零转义**：字符串内容原样书写，引号、反斜杠、括号、任意符号都不需要处理。唯一的例外是键名，见第 3 节。
- **格式上不可注入**：内容行永远带有固定前缀 `|`，只有结构行能出现在结构位置。
- **无类型**：格式只分帧，类型由 schema 解释。
- **可流式**：每一行的含义只由它所在的环境、缩进和首字符决定，不需要向后看。
- **可检测截断**：消息必须以结束符结束，流中途断开会被识别为不完整。
- **适合手写**：没有引号、括号配对和转义，结构靠缩进。

本规范中，“必须”“禁止”表示强制要求，“建议”表示推荐做法。

## 2. 基本约定

一条 LLF 消息由若干行组成，以结束符结束。顶层可以是隐式键值对，也可以是一个单独的值。

**编码**

- 文本必须是 UTF-8，禁止带 BOM。

**行**

- 行只以 LF（`\n`）分隔。
- **默认宽松模式**：解析前把 CRLF 转为 LF，便于手写。宽松模式下，行尾带 CR 的内容无法表示。
- **严格模式**（可选）：不做任何换行符转换，CR 是普通字符，从而保证任意字符串都能无损往返。

**缩进**

- 每层缩进恰好 2 个空格，顶层为 0 格。
- 缩进中禁止出现 tab。子行必须恰好比父行多 2 格，多或少都是错误。

**消息结束**

- 结束符是空行：最后一个内容行末尾的 LF 之后紧跟一个 LF（即 `\n\n`）。解析器读到第一个 LF 时只能确定当前行结束，必须看到下一个字符才能判断：若是 LF 则消息结束，否则是下一行的开始。任何实现都禁止在第一个 LF 处就判定消息结束，否则流式生成时会误截断。
- 只含空白字符的行是错误，不是结束符。
- 到达输入末尾（EOF）却没有遇到结束符，消息视为被截断，必须报错。
- 一个流中可以依次传输多条消息，每条以结束符结束。
- 只有一个空行的消息（单个 `\n`）表示空的顶层键值对。

**顶层形态**

- 第一个非注释行决定整条消息的形态：**它的第一个 token 是头**（`-`、`_`、`{}`、`[]`）时，顶层是一个单独的值；否则顶层是隐式键值对，该行是第一条条目行。
- 顶层单独值是必要的：JSON 的顶层可以是任何类型，工具返回也不总是字典。它只能出现一次，之后再出现任何非注释行都是错误。
- 由于普通键名不能恰好是这四个头，而引号键以 `"` 开头，第一行即可确定形态，流式解析不需要向后看。
- 工具调用建议使用键值对形态；工具结果可以用单独值，比如直接返回一段文本或一个列表。

```
[]
  - src/main.py
  - src/util.py

```

**大小与深度**

- 本规范不规定嵌套深度或消息大小的上限。实现可以自行设限，但那是实现策略，不是格式语义。

## 3. 行的种类与环境

除注释行外，每一行的种类由它所在的**环境**唯一决定。环境来自父级的头：

| 环境 | 子行只能是 |
| --- | --- |
| 顶层隐式键值对，或 `{}` 的子层 | 条目行（键 + 头） |
| `[]` 的子层，或顶层单独值 | 项行（只有头，没有键） |
| `-` 的子层 | 文本行（`\|` 开头） |

四种行：

| 行的种类 | 形式 | 出现位置 |
| --- | --- | --- |
| 条目行 | 缩进 + 键 + 空格 + 头 | 顶层隐式键值对，或 `{}` 的子层 |
| 项行 | 缩进 + 头 | `[]` 的子层，或顶层单独值 |
| 文本行 | 缩进 + `\|` + 内容 | `-` 的子层 |
| 注释行 | 任意空格 + `#` + 任意内容 | 除文本块内部以外的任何位置 |

**键名**

- **普通键名**：非空、不含空格、不含 tab、不含双引号 `"`，且不等于四个头之一（`-`、`_`、`{}`、`[]`）。除此之外没有字符集限制，允许非 ASCII（Unicode）字符。例如 `名字`、`dry-run`、`_private`、`a.b`、`@x`、`a#b` 都合法。
- **引号键名（逃生口）**：`"…"`，其中转义规则与 JSON 字符串完全相同（`\"`、`\\`、`\/`、`\b`、`\f`、`\n`、`\r`、`\t`、`\uXXXX`）。它用来写含空格、tab、双引号、换行或空字符串的键，也用来写恰好是四个头的键：`"a b"`、`"a\"b"`、`""`、`"-"` 都是合法键名。
- 键名和头之间恰好一个空格。普通键名不含空格，所以第一个空格就是分隔符；引号键名先解析到闭合引号，再要求恰好一个空格。
- 同一层级内键名禁止重复，重复即报错。键名按**码点序列**比较，不做 Unicode 归一化（与 JSON 相同）；建议生成方输出 NFC 形式，避免视觉相同、码点不同的重复键。
- 键的书写顺序必须保留。
- 条目行只有键、没有头，是错误。以 `#` 开头的行是注释，因此普通键名不能以 `#` 开头，需要时用引号键名。

**注释**

- 以 `#` 开头（前面可以有任意空格）的行是注释，整行忽略，不参与缩进校验。
- 注释只能独占一行，没有行尾注释。`key - a # b` 的值就是 `a # b`。
- 注释禁止出现在文本块中间，原因见第 5 节。

由于头只在行首占一个 token、普通键名不含双引号、引号键以双引号开头，四种行在任何环境中都不会混淆。

## 4. 头与值

| 头 | 含义 | 同行载荷 | 子层 | 什么都不写时 |
| --- | --- | --- | --- | --- |
| `-` | 字符串 | 可选，单行文本 | 可选，文本行 | 空字符串 |
| `_` | null | 禁止 | 禁止 | 即 null |
| `{}` | 字典 | 禁止 | 可选，条目行 | 空字典 |
| `[]` | 列表 | 禁止 | 可选，项行 | 空列表 |

**规则**

- `-` 后的第一个空格是分隔符，其余部分原样作为值，只去掉行尾的空格和 tab。
- 同行载荷和子层文本行不能同时出现。
- `_`、`{}`、`[]` 之后除行尾空白外不得有内容，也不能有文本子层。
- 字符串不带结构子层：`-` 的子层只能是文本行。
- 需要保留首尾空白或换行的值，必须用多行形式书写。

**四种“空”**

| 写法（字典环境） | 值 |
| --- | --- |
| `key -` | 空字符串 |
| `key _` | null |
| `key {}` | 空字典 |
| `key []` | 空列表 |

在列表环境或顶层单独值里去掉键：`-`、`_`、`{}`、`[]`。

**缺省与 null 是两件事**

- 键不出现 = 缺省。
- `key _` = 显式 null。
- 键出现但没有头 = 错误，不是 null。

## 5. 多行文本

多行文本是 `-` 下方连续的文本行，每行以 `|` 开头，`|` 之后直到行尾的全部字符原样保留。

**规则**

- 文本行的缩进必须恰好比 `-` 所在行多 2 格。
- 每行内容 = `|` 之后的全部字符，包括首尾空格、tab、`|`、`#` 等，不做任何处理。
- 单独一个 `|` 表示一个空内容行。
- 值 = 所有内容行用 LF 连接，末尾不附加换行。
- 需要末尾换行时，在最后多写一行单独的 `|`。
- 文本块在遇到第一个非文本行（条目行、项行、注释行或空行）时立即结束。之后再出现属于同一个值的文本行，是错误。

**为什么文本块不能被打断**

多行字符串可能包含任意内容，所以它必须是一个连续、不可分割的整体。注释或任何其他格式元素都不能插入其中，文本块从第一个 `|` 行开始，到第一个非 `|` 行结束，中间只允许 `|` 行。

这条规则还带来一个实际好处：代码里有大量以 `#` 开头的行，如果模型漏写了某行的 `|`，这一行会结束文本块，后续的 `|` 行触发 E11 报错，而不是被当成注释悄悄丢掉。

**示例**

```
script -
  |def f():
  |    return "x - y" # 不是注释
  |
  |print(f())
  |

```

值为 `def f():\n    return "x - y" # 不是注释\n\nprint(f())\n`，末尾带一个换行。

**编码算法**

对任意字符串 s：按 LF 切分为若干行，每行写成“缩进 + `|` + 该行”。这个算法对所有字符串都有效，不需要检查内容。空字符串建议直接写 `-`。

## 6. 与 schema 的关系

- 规范**不定义 schema 语言**。类型用 **JSON Schema**（或任何等价的类型描述）给出，因为 LLF 的结构与 JSON 一一对应，没有必要再造一套。
- 格式只产出四种东西：字典、列表、字符串、null。`-` 的载荷永远是字符串，由格式**原样**交出，不做任何转换。
- JSON Schema 的类型与 LLF 的对应：`object` → `{}`，`array` → `[]`，`null` → `_`，`string` / `integer` / `number` / `boolean` → `-` 的字符串载荷，由 schema 解释。
- 没有 schema 时，所有 `-` 载荷当字符串、`_` 当 null。**信息不丢**：字符串原文完整保留，之后随时可以用 schema 重新解释成整数、布尔等。
- schema 的解释失败（例如 `- abc` 遇上 `integer`）是 schema 层的错误，不是格式错误。

## 7. 流式生成与解析

LLF 的每一行只凭自身所处的环境、缩进和行首字符就能确定含义，因此解析器可以逐行、甚至逐字符地输出结果，不需要缓冲整条消息。

**逐行**

解析器每读完一行就产生一个事件，例如：进入字典、键 + 字符串、开始字符串、文本行、结束当前层级、消息结束。上层可以随事件立即行动。

**逐字符**

- 一个文本行一旦读到行首的 `|`，其后的每个字符都可以立即交给上层，直到 LF。行内没有任何字符能改变已输出内容的含义。
- 单行 `-` 值要去掉行尾空白，所以末尾的空格和 tab 需要暂存到 LF 出现为止。

**字段顺序**

- 建议把大段文本字段放在消息最后。这样接收方先拿到其余参数，比如先得到文件路径并打开文件，再边接收边写入内容。
- 工具的 schema 可以把某个字符串字段声明为“流式字段”，此时该字段必须是同层最后一个键。

**提交语义**

- 流式处理产生的副作用建议先作为临时结果，在读到结束符后再提交。例如先写入临时文件，收到结束符后再重命名为目标文件。
- 如果流在结束符之前中断，消息被判定为截断，临时结果应当丢弃。

## 8. 不可注入性与已知限制

由符合规范的编码器写出的内容，不可能被解析成结构。

**论证**

1. 编码器把内容的每一行都写成“固定缩进 + `|` + 原文”。
2. 解析器只根据环境、缩进和行首字符判定行的种类。位于该缩进、以 `|` 开头的行，只可能是文本行。
3. 内容只出现在 `|` 之后，无法影响这一行的缩进和行首字符，所以无法改变行的种类。
4. 内容中的空行被编码为单独的 `|`，因此内容永远不会产生空行，也就无法提前结束消息。
5. 因此对任意字符串 s，都有 decode(encode(s)) = s。实现应当用模糊测试验证这一性质。

注意这条保证的边界：它说的是**编码器写出的内容**。如下一段所述，格式管不了别的。

**格式管不到的风险**

- 当内容由模型抄写而不是由程序拼接时，模型可能漏写某行的 `|`，或被提示词注入诱导写出伪造的结构行。这是模型层面的问题，任何格式都无法避免。
- 缓解办法：外部内容尽量由程序拼接，而不是让模型转抄；解析后再用 schema 校验，未知的键直接报错。

**已知限制**

- 会删除行尾空白的工具或传输通道，会改变以空白结尾的文本行内容。结构不会被破坏，但内容不再精确；格式层没有补救手段，需要无损时只能换通道或由程序另行编码。
- 默认宽松模式把 CRLF 转为 LF，因此无法表示行尾带 CR 的内容；需要无损时用严格模式。
- 模型可能数错缩进。严格的解析规则把这类错误变成报错，而不是误解析。

## 9. 用于模型工具调用

建议每次工具调用是一条 LLF 消息：`call` 给出工具名，`args` 给出参数，大段文本参数放在最后以便流式处理。多个调用依次输出多条消息即可。参数的类型由工具的 JSON Schema 解释，消息里一律是字符串。

**示例**

```
# 写文件：类型由 schema 解释
call - write_file
id - c1
args {}
  path - src/main.py
  mode - 644
  overwrite - true
  backup _
  tags []
    - draft
    {}
      owner - alice
  content -
    |def f():
    |    return "call - fake"
    |
    |print(f())
    |

```

最后的空行结束消息。`content` 里的 `call - fake` 只是普通文本；`mode - 644` 与 `overwrite - true` 是字符串，schema 把它们读成整数和布尔。

**引号键的用途**

参数名里含空格或特殊字符时用引号键：

```
args {}
  "full name" - 张三
  "Content-Type" - application/json
  "" - 空键也可以

```

**工具结果**

工具返回的结果同样可以用 LLF 编码，而且**不总是字典**：顶层单独值可以直接返回一段文本、一个列表或一个 null。结果由程序机械编码，外部内容即使包含伪造的结构，也只会成为文本行。

```
- src/main.py
- src/util.py

```

**系统提示词片段**

```
用 LLF 格式输出工具调用：
- 顶层是键值对，每行 "键 一个空格 头"。
- 头：- 字符串，_ null，{} 字典，[] 列表。
- 字典的子行是 "键 头"；列表的子行只有头，没有键。
- 键名不能含空格与双引号；需要时用 JSON 风格的 "引号键"。
- 多行字符串：写 "键 -"，下一层每行以 | 开头，| 后原样书写，不要转义。
- 空串写 "-"，null 写 "_"，空字典写 "{}"，空列表写 "[]"。
- 所有值都写成字符串，不要写类型标记；类型由参数 schema 解释。
- 缩进每层 2 空格。
- 以一个空行结束调用。
```

**与 JSON 的对比**

两者都能表达同一组结构，区别在**长文本的失败模式**，不在表达能力：

| 方面 | JSON | LLF |
| --- | --- | --- |
| 长文本 | 引号、反斜杠、换行都要转义，转义出错会静默改变内容 | 原样书写，行前缀保证内容变不成结构 |
| 转义成本 | 编码器必须识别并处理特殊字符 | 编码器对任意内容都是同一套算法（只有键名可能要用引号） |
| 流式 | 需要增量解析器 | 逐行天然可解析 |
| 截断检测 | 括号不闭合时可检测 | 缺少结束符即可检测 |
| 类型 | 字面形式带类型（number/bool/null 各自成型） | 只有字符串，类型由外部 JSON Schema 解释 |
| 生态 | 成熟 | 需要新实现 |

建议用实测数据支撑推广：在长代码参数的工具调用上，对比 JSON 与 LLF 的解析失败率、内容错误率和 token 数。

## 10. 与 JSON 的映射

LLF 的结构与 JSON 一一对应，差别只在标量：LLF 没有数字与布尔的字面形式，它们都是字符串，由 schema 解释。

**LLF 到 JSON（无 schema 时）**

| LLF | JSON |
| --- | --- |
| `- 载荷` | string |
| `_` | null |
| `{}` | object，保留键顺序 |
| `[]` | array |

有 schema 时，先按 JSON Schema 把每个 `-` 字符串解释成声明类型（integer、number、boolean 等），再转 JSON。

**JSON 到 LLF**

- null → `_`。
- string → `- 载荷`；含 LF 或首尾空白的字符串用多行形式。
- array → `[]`，元素依次写成项行。
- object → 隐式键值对（顶层）或 `{}`（嵌套）。
- number / bool → `- 文本`（如 `- 3`、`- true`）；类型由 JSON Schema 恢复。
- 顶层是 object → 隐式键值对；是其他任意值（array、string、number、bool、null）→ 顶层单独值。这是顶层单独值必须保留的原因，也是工具结果不一定是字典时的落点。
- 键名：普通键与引号键合起来可以表示任意 JSON 键（含空键、含空格、含双引号、含换行、恰好是四个头名）。
- **单独看一份 LLF 而没有 schema 时，number / string 的区别无法从格式里恢复**；但内容不丢，接上 schema 即可还原。这是把类型交给 schema 的必然结果，不是缺陷。

## 11. 错误情况与测试向量

解析器遇到以下任何情况都必须报错，禁止容错修复。

| 代码 | 错误 |
| --- | --- |
| E01 | 带 BOM，或出现只含空白的行 |
| E02 | 到达输入末尾仍无结束符（截断） |
| E03 | 缩进不是恰好多 2 格，或缩进中含 tab |
| E04 | 条目行只有键，没有头 |
| E05 | 普通键名为空、含空格/tab/双引号，或恰好是四个头之一；或键与头之间不是恰好一个空格 |
| E06 | 同一层级键名重复 |
| E07 | 未知的头符号；或该位置出现了头而这里需要键（列表项出现在字典环境） |
| E08 | `_` / `{}` / `[]` 带同行载荷；或 `{}` / `[]` 下出现文本行 |
| E09 | `_` 下出现子层 |
| E10 | `-` 同时带同行载荷和子层；或它的子层里出现结构行 |
| E11 | 文本行出现在不允许的位置，或文本块结束后又出现文本行 |
| E12 | 顶层单独值之后又出现非注释行 |
| E13 | 引号键中出现非法转义序列 |
| E14 | 引号键没有闭合引号 |

**测试向量**（`↵` 表示 LF，`⇥` 表示一个 tab 字符；期望值是“无 schema”下的解析结果）

| # | 输入 | 期望结果 |
| --- | --- | --- |
| 1 | `a - hello↵↵` | `{"a": "hello"}` |
| 2 | `a - x # y↵↵` | `{"a": "x # y"}` |
| 3 | `a - x  ↵↵` | `{"a": "x"}`，行尾空白去掉 |
| 4 | `a -↵  \|x  ↵↵` | `{"a": "x  "}`，文本行保留空白 |
| 5 | `a -↵  \|call - fake↵↵` | `{"a": "call - fake"}` |
| 6 | `a -↵  \|x↵  \|↵↵` | `{"a": "x\n"}` |
| 7 | `a -↵  \|↵  \|↵↵` | `{"a": "\n"}` |
| 8 | `a _↵↵` | `{"a": null}` |
| 9 | `a []↵  - s↵  - 2↵  _↵↵` | `{"a": ["s", "2", null]}` |
| 10 | `a {}↵↵` | `{"a": {}}` |
| 11 | `d - 2026-09-19↵↵` | `{"d": "2026-09-19"}`，是否读成日期属于 schema |
| 12 | `↵` | `{}` |
| 13 | `[]↵  - a↵  - b↵↵` | `["a", "b"]` |
| 14 | `-↵  \|x↵↵` | `"x"` |
| 15 | `# c↵- 3↵↵` | `"3"` |
| 16 | `- -↵↵` | `"-"`，第一个 token 是头，所以是顶层单独值 |
| 17 | `_↵↵` | `null` |
| 18 | `a -↵↵` | `{"a": ""}` |
| 19 | `a - 007↵↵` | `{"a": "007"}`，格式不做任何规范化 |
| 20 | `a {}↵  b - 1↵↵` | `{"a": {"b": "1"}}` |
| 21 | `a []↵  {}↵    b - 1↵↵` | `{"a": [{"b": "1"}]}` |
| 22 | `a - x` | E02 |
| 23 | `a↵↵` | E04 |
| 24 | `a % 007↵↵` | E07，`%` 不是头 |
| 25 | `a - x↵a - y↵↵` | E06 |
| 26 | `a -↵   \|x↵↵` | E03 |
| 27 | `a -↵  \|x↵  # c↵  \|y↵↵` | E11 |
| 28 | `- 3↵a - x↵↵` | E12 |
| 29 | `a {}↵  - - v↵↵` | E05，键不能恰好是 `-` |
| 30 | `a _↵  b - 1↵↵` | E09 |
| 31 | `a []↵  - x↵  b - 1↵↵` | E07 |
| 32 | `a -↵  \|# 不是注释↵↵` | `{"a": "# 不是注释"}` |
| 33 | `d @date 2026-09-19↵↵` | E07，`@date` 不是头 |
| 34 | `名字 - 柚子↵↵` | `{"名字": "柚子"}`，允许非 ASCII 键名 |
| 35 | `@x - v↵↵` | `{"@x": "v"}`，`@` 只是普通字符 |
| 36 | `a#b - v↵↵` | `{"a#b": "v"}` |
| 37 | `"a b" - v↵↵` | `{"a b": "v"}`，引号键含空格 |
| 38 | `"" - v↵↵` | `{"": "v"}`，空键 |
| 39 | `"-" - v↵↵` | `{"-": "v"}`，四个头名之一也能用引号键写 |
| 40 | `"a\"b" - v↵↵` | `{"a\"b": "v"}`，键是 `a"b` |
| 41 | `"a\tb" - v↵↵` | `{"a\tb": "v"}`，键含 tab |
| 42 | `"#foo" - v↵↵` | `{"#foo": "v"}` |
| 43 | `"a\x" - v↵↵` | E13 |
| 44 | `"a - v↵↵` | E14 |
| 45 | `a"b - v↵↵` | E05，普通键名不能含双引号 |
| 46 | `a⇥b - v↵↵` | E05，普通键名不能含 tab |

正式发布时，测试向量应另存为机器可读的文件，每条包含原始字节和期望值或错误代码。

## 12. 命名与版本变化

**为什么改名（v0.4）**

格式不做任何类型解释，类型已经完全交给 schema。旧名 `Line-Typed Format` 里的 “Typed” 会让每个新读者以为值带类型，和格式的主张相反。新名 **LLF = Literal Line Format（字面行格式）** 落在两个删不掉的性质上：**零转义（literal）** 与 **行结构（line）**。扩展名 `.llf`，MIME `text/llf`。

**v0.1 → v0.2**

- 删除标量类型符号 `~` `%` `/` `?`：它们不再是合法头，出现即 E07。
- `_` 从类型符号改成头，表示 null 值。
- `-` 成为标量头。
- 行的种类由父头所处的环境决定：`{}` 的子层是条目行，`[]` 的子层是项行。
- 键名不能恰好是 `-` 或 `_`。
- 标量类型交给 schema；格式不再解析整数、浮点、布尔。

**v0.2 → v0.3**

- 删除逃生口 `@name`：所有标量本来就是字符串，类型解释已经在 schema 侧。
- 数据模型明确为字典、列表、字符串、null。
- 文本行只出现在 `-` 之下；头收敛为四个。

**v0.3 → v0.4**

- 改名 LTF → LLF（见上）。
- 键名允许任意非 ASCII 字符。
- 明确不规定嵌套深度与消息大小的上限。
- 明确顶层单独值必须保留。
- 明确 null 允许，由 `_` 表示。
- 明确不定义 schema 语言，直接复用 JSON Schema。
- 明确无 schema 时值一律为字符串，信息不丢。

**v0.4 → v0.5**

- 新增引号键逃生口：JSON 风格的 `"…"` 加常规转义。含空格、tab、双引号、换行、空字符串，以及恰好是四个头名的键，现在都能表示。
- JSON 键映射的最后一个缺口补上。
- 宽松模式（CRLF→LF）改为默认；严格模式用于逐字节无损往返。
- 键名按码点序列比较，不做 Unicode 归一化；建议生成方输出 NFC。
- 与 NestedText 的差异改述为「字典与列表的表示方式」：LLF 显式写 `{}` / `[]`，嵌套与空容器无歧义。

**v0.5 → v0.6**

- 加入参考实现：`llf.py`（解析器 + 编码器）、机器可读测试向量 `vectors.json`、测试 `tests/test_llf.py`（46 条向量 + 2000 组往返 fuzz）。
- 不改变格式语义。

## 13. 待定问题

- [ ] 是否把 `text/llf`、`.llf` 注册到权威的 MIME / 扩展名表。
- [ ] 是否有其它语言需要参考实现（当前只有 Python）。
