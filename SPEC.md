# LLF 字面行格式规范 v0.11 草案

2026-09-21

原名 LTF（Line-Typed Format），v0.4 起改名为 LLF（Literal Line Format，字面行格式）。改名理由见第 12 节。

## 1. 概述与设计目标

LLF 是一种以行为单位的数据格式，主要面向大模型的工具调用，也适合手写。

格式本身**不写类型、不做类型判断**。它只存四样东西：字典（`{}`）、列表（`[]`）、字符串（`-`）、null（`_`）。**除 `_` 这个 null 记号外，所有标量一律是字符串**；格式里没有别的类型标记，也没有类型逃生口。某段字符串该被读成整数、浮点、布尔、日期还是别的什么，是**使用者自己的事**——那是格式之外的扩展，LLF 本体不规定。JSON Schema 只是这层解释最顺手的一种选择，见第 6 节（非规范性附录）。

设计目标：

- **零转义**：字符串内容原样书写，引号、反斜杠、括号、任意符号都不需要处理。唯一的例外是键名，见第 3 节。
- **格式上不可注入**：内容行永远带有固定前缀 `|`，只有结构行能出现在结构位置。
- **核心最小、类型外置**：格式只分帧；类型解释属于使用者的扩展，不在格式里。
- **可流式**：每一行的含义只由它所在的环境和首字符决定（只有结构行另外用缩进定层级），不需要向后看。
- **可检测截断**：消息必须以结束符结束，流中途断开会被识别为不完整。
- **适合手写**：没有引号、括号配对和转义，结构靠缩进。

本规范中，“必须”“禁止”表示强制要求，“建议”表示推荐做法。

## 2. 基本约定

一条 LLF 消息由若干行组成，以结束符结束。顶层可以是隐式键值对，也可以是一个单独的值。

**编码**

- 文本必须是 UTF-8，禁止带 BOM。

**行**

- 行只以 LF（`\n`）分隔。
- **默认宽松模式**：解析前把 CRLF 转为 LF，便于手写。宽松模式下，行尾带 CR 的内容无法表示。
- **严格模式**（可选）：不做任何换行符转换，CR 因而可以作为内容保留，任意字符串都能无损往返。唯一的例外是 `-` 的行内载荷按第 4 节 strip（CR 属于 `White_Space`），需要逐字节保留时用文本块书写。

**缩进**

- 结构行每层缩进恰好 2 个空格，顶层为 0 格；文本行的 `|` 可以顶格、可以缩进任意个空格（第 3、5 节）。
- 缩进中禁止出现 tab。结构行必须恰好比父行多 2 格，多或少都是错误。

**消息结束**

- 结束符是单独一行：去掉首尾 `White_Space` 后，整行内容恰好是 `--LLF-END`。它是可见的字面 token，不会被“删除行尾空白”的工具弄丢。
- 结束符行必须是消息的最后一行；消息体到它之前为止。只有结束符一行的消息表示空的顶层键值对。
- 消息体里只含空白字符的行是错误（E01），不是结束符，也不是内容。
- 到达输入末尾（EOF）却没有遇到结束符，消息视为被截断，必须报错（E02）。
- 一个流中可以依次传输多条消息，每条以结束符结束，结束符之后紧跟下一条消息的第一行。
- 解析器读到结束符行即可判定消息结束，不必在行尾向后多看字符。结束符之后的内容属于谁，是**调用层**的选择：单条消息入口（如 `parse`）把它当 E12，消息流入口（如 `parse_multi`）把它当作下一条消息；格式本身不预设。

**顶层形态**

- 第一个非注释行决定整条消息的形态：**它的第一个 token 是头**（`-`、`_`、`{}`、`[]`）时，顶层是一个单独的值；否则顶层是隐式键值对，该行是第一条条目行。
- 顶层单独值是必要的：JSON 的顶层可以是任何类型，工具返回也不总是字典。它只能出现一次，之后再出现任何非注释行都是错误。
- 由于普通键名不能恰好是这四个头，而引号键以 `"` 开头，第一行即可确定形态，流式解析不需要向后看。
- 工具调用建议使用键值对形态；工具结果可以用单独值，比如直接返回一段文本或一个列表。

```
[]
  - src/main.py
  - src/util.py

```

**大小与深度**

- 本规范不规定嵌套深度或消息大小的上限。实现可以自行设限，但那是实现策略，不是格式语义。

## 3. 行的种类与环境

除注释行与文本行外，每一行的种类由它所在的**环境**唯一决定。环境来自父级的头：

| 环境 | 子行只能是 |
| --- | --- |
| 顶层隐式键值对，或 `{}` 的子层 | 条目行（键 + 头） |
| `[]` 的子层，或顶层单独值 | 项行（只有头，没有键） |
| `-` 之后（文本块） | 文本行（`\|` 开头） |

文本行是例外：它不参与缩进层级，由“紧跟在 `-` 行之后、连续的 `\|` 行”界定，前导空格一律忽略（第 5 节）。

四种行：

| 行的种类 | 形式 | 出现位置 |
| --- | --- | --- |
| 条目行 | 缩进 + 键 + 空格 + 头 | 顶层隐式键值对，或 `{}` 的子层 |
| 项行 | 缩进 + 头 | `[]` 的子层，或顶层单独值 |
| 文本行 | 任意空格 + `\|` + 内容 | `-` 行之后（无行内载荷） |
| 注释行 | 任意空格 + `#` + 任意内容 | 除文本块内部以外的任何位置 |

`-` 之后的文本块里，`\|` 之前的空格一律忽略，所以文本行可以顶格，也可以缩进任意个空格；只有结构行（条目行、项行）才受“恰好比父行多 2 格”的约束。`\|` 之后直到下一个 LF 的全部字符才是内容。文本行只按“紧跟 `-` 的连续 `\|` 行”界定，不参与缩进层级。

**行的归类先于键名校验**

一行的第一个 token 是头（`-`、`_`、`{}`、`[]`）时，该行按**项行**处理；否则按**条目行**处理。这与第 2 节判定顶层形态的规则一致。由此：

- 字典环境里出现项行 → E15。
- 列表环境里出现不以头开头的行 → E15。

普通键名不能恰好是某个头，这条书写规则仍然成立，但违反它的行会先被归类为项行，因此报 E15 而不是 E05。需要以头为名的键必须用引号键。

**键名**

- **普通键名**：非空、不含任何 `White_Space` 字符（空格、tab、CR、LF、NBSP、全角空格 U+3000 等，定义见第 4 节）、不含双引号 `"`，不以 `|` 开头（那会被当成文本行），且不等于四个头之一（`-`、`_`、`{}`、`[]`）。除此之外没有字符集限制，允许非 ASCII（Unicode）字符。例如 `名字`、`dry-run`、`_private`、`a.b`、`@x`、`a#b` 都合法。含空白的键必须用引号键。
- **引号键名（逃生口）**：`"…"`，其中转义规则与 JSON 字符串完全相同（`\"`、`\\`、`\/`、`\b`、`\f`、`\n`、`\r`、`\t`、`\uXXXX`）。它用来写含空格、tab、双引号、换行或空字符串的键，也用来写恰好是四个头的键、以 `|` 开头的键：`"a b"`、`"a\"b"`、`""`、`"-"`、`"|"` 都是合法键名。
- 键名和头之间恰好一个 U+0020。普通键名不含 U+0020，所以第一个 U+0020 就是分隔符；引号键名先解析到闭合引号，再要求恰好一个 U+0020。行内分词只认 U+0020：NBSP、全角空格 U+3000 等其它空白不是分隔符（它们只能出现在引号键或 `-` 载荷里）。
- 同一层级内键名禁止重复，重复即报错。键名按**码点序列**比较，不做 Unicode 归一化（与 JSON 相同）；建议生成方输出 NFC 形式，避免视觉相同、码点不同的重复键。
- 重复判定发生在**解引号之后**：引号键先按 JSON 转义规则解码，再与普通键比较。因此同一层级中 `"a" - 1` 与 `a - 2` 是重复键，报 E06。
- 键的书写顺序必须保留。
- 条目行只有键、没有头，是错误。以 `#` 开头的行是注释，因此普通键名不能以 `#` 开头，需要时用引号键名。

**注释**

- 以 `#` 开头（前面可以有任意空格）的行是注释，整行忽略，不参与缩进校验。
- 注释只能独占一行，没有行尾注释。`key - a # b` 的值就是 `a # b`。
- 注释禁止出现在文本块中间，原因见第 5 节。

由于头只在行首占一个以 U+0020 分隔的 token、普通键名不含双引号、引号键以双引号开头，四种行在任何环境中都不会混淆。

## 4. 头与值

| 头 | 含义 | 同行载荷 | 子层 | 什么都不写时 |
| --- | --- | --- | --- | --- |
| `-` | 字符串（两端空白去掉） | 可选，单行文本 | 可选，文本行 | 空字符串 |
| `_` | null | 禁止 | 禁止 | 即 null |
| `{}` | 字典 | 禁止 | 可选，条目行 | 空字典 |
| `[]` | 列表 | 禁止 | 可选，项行 | 空列表 |

**规则**

- `-` 必须是一个以 U+0020 分隔的完整 token：其后紧跟 U+0020 或行尾。满足后，`-` 之后的全部字符是行内载荷，**两端按 `White_Space` 去掉**（strip）。`-` 单独成行、或其后只有空白时，载荷都是空字符串。因此 `a -\tvalue`（tab 紧跟在 `-` 后）不是合法的 `-` 头，报 E07。
- 这里的“两端空白”指 Unicode `White_Space` 属性为真的全部码点（空格、tab、CR、NBSP、全角空格 U+3000 等）。规范按属性定义，不按某个语言库的实现定义，以免跨语言实现产生分歧。注意：这说的是 `-` 载荷两端的 strip；行内分词（键/头分隔、头 token 边界）只认 U+0020。
- 同行载荷和文本块不能同时出现。
- `_`、`{}`、`[]` 之后除行尾空白外不得有内容，也不能有文本块。
- `-` 之后的文本块只能由文本行组成，不能出现结构行。
- 需要保留首尾空白或换行的值，用文本块书写；文本块可以只有一行（第 5 节）。

**四种“空”**

| 写法（字典环境） | 值 |
| --- | --- |
| `key -` | 空字符串 |
| `key _` | null |
| `key {}` | 空字典 |
| `key []` | 空列表 |

在列表环境或顶层单独值里去掉键：`-`、`_`、`{}`、`[]`。

**缺省与 null 是两件事**

- 键不出现 = 缺省。
- `key _` = 显式 null。
- 键出现但没有头 = 错误，不是 null。

## 5. 多行文本

多行文本是 `-` 下方连续的文本行，每行以 `|` 开头，`|` 之后直到行尾的全部字符原样保留。

**规则**

- 文本行可以顶格，也可以缩进任意个空格：`\|` 之前的空格一律忽略，不是内容。内容从 `\|` 之后开始。
- 每行内容 = `|` 之后的全部字符，包括首尾空格、tab、`|`、`#` 等，不做任何处理。
- 单独一个 `|` 表示一个空内容行。
- 值 = 所有内容行用 LF 连接，末尾不附加换行。
- 需要末尾换行时，在最后多写一行单独的 `|`。
- 文本块在遇到第一个非文本行（条目行、项行、注释行、空行或结束符行）时立即结束。之后再出现属于同一个值的文本行，是错误。

**为什么文本块不能被打断**

多行字符串可能包含任意内容，所以它必须是一个连续、不可分割的整体。注释或任何其他格式元素都不能插入其中，文本块从第一个 `|` 行开始，到第一个非 `|` 行结束，中间只允许 `|` 行。

这条规则还带来一个实际好处：代码里有大量以 `#` 开头的行，如果模型漏写了某行的 `|`，这一行会结束文本块，后续的 `|` 行触发 E11 报错，而不是被当成注释悄悄丢掉。

**示例**

```
script -
  |def f():
  |    return "x - y" # 不是注释
  |
  |print(f())
  |

```

值为 `def f():\n    return "x - y" # 不是注释\n\nprint(f())\n`，末尾带一个换行。

**编码算法**

编码器按以下顺序选择形式：

1. 内容含 LF，或内容首尾有 `White_Space` 空白 → 文本块：按 LF 切分为若干行，每行写成“缩进 + `|` + 该行”。编码器固定按父行 +2 格排版，解码器不检查文本行的缩进。文本块可以只有一行。
2. 其余 → 行内 `-`。

这是一次内容检查，不是转义。

## 6. 与外部类型描述的关系（非规范性附录）

本节是**非规范性附录**：它讨论“把 LLF 交出的字符串解释成具体类型”这层可选扩展，不属于格式本体；不读本节也能实现 LLF。

**格式与 schema 解耦**

LLF 只做一件事：把结构无歧义地交出来。它**不依赖 JSON，也不依赖 JSON Schema，更不规定用哪种 schema 语言**——XSD、protobuf、Avro、TypeScript 类型、自家 DSL 都可以，选哪个是使用者的自由。本规范推荐 JSON Schema，只是因为 LLF 的四种值（字典、列表、字符串、null）与 JSON 一一对应，用起来最省事；这不是格式的要求。

**为什么格式不定义类型与词法**

这是刻意的解耦，不是遗漏。任何校验语言都只声明“此处应为 integer”，而不规定字符串 `"644"` 如何变成整数、`"1e3"` 算不算 integer、`"007"` 是否保留前导零、`"True"` / `"true"` 算不算 boolean、浮点按哪种规则解析；JSON Schema 只是其中最常被追问的那个例子。把这些写进 LLF 会牺牲三件事：

- **一份格式、多个消费者。** 同一份 LLF 可能被不同工具、不同语言、不同版本的 schema 读取；格式不该替所有消费者统一转换语义。
- **格式要小而稳。** 一旦 LLF 承担词法（lexical space），它就得跟着各语言的数字/布尔解析差异、精度与地区写法一起演化——那不是分帧格式该背的负担。
- **复用而非重造。** 类型描述已有一堆成熟选择，格式不必再造一套，也不必把自己绑定到其中任何一个。

因此：**LLF 只负责无歧义地交出字符串原文，不定义 integer / number / boolean 的词法空间，也不定义字符串到这些类型的转换。** 需要严格转换的调用方，在自己的**绑定层**（LLF → 具体函数调用）定义词法（例如正则）与错误语义，并明确“不在词法空间内即绑定层错误”。不同绑定层可以不同，但都建立在同一份字符串原文之上。

**具体约定**

- 规范**不定义 schema 语言，也不要求 schema 是 JSON Schema**。类型描述由调用方自选。
- 格式只产出四种东西：字典、列表、字符串、null。`-` 的载荷永远是字符串，由格式**原样**交出，不做任何转换。
- 若选用 JSON Schema，对应关系是：`object` → `{}`，`array` → `[]`，`null` → `_`，`string` / `integer` / `number` / `boolean` → `-` 的字符串载荷，由 schema 解释。
- 没有 schema 时，所有 `-` 载荷当字符串、`_` 当 null。**信息不丢**：字符串原文完整保留，之后随时可以用 schema 重新解释成整数、布尔等。
- schema 的解释失败（例如 `- abc` 遇上 `integer`）是 schema 层的错误，不是格式错误。

## 7. 流式生成与解析

LLF 的每一行只凭自身所处的环境、缩进和行首字符就能确定含义，因此解析器可以逐行、甚至逐字符地输出结果，不需要缓冲整条消息。

**逐行**

解析器每读完一行就产生一个事件，例如：进入字典、键 + 字符串、开始字符串、文本行、结束当前层级、消息结束。上层可以随事件立即行动。

**逐字符**

- 一个文本行一旦读到 `|`（前导空格忽略），其后的每个字符都可以立即交给上层，直到 LF。行内没有任何字符能改变已输出内容的含义。
- 读到结束符行 `--LLF-END` 时立即产生“消息结束”事件；它不依赖行尾空白。
- 单行 `-` 值 strip 两端，所以首尾的空白需要暂存到 LF 出现为止。

**字段顺序**

- 建议把大段文本字段放在消息最后。这样接收方先拿到其余参数，比如先得到文件路径并打开文件，再边接收边写入内容。
- 工具的 schema 可以把某个字符串字段声明为“流式字段”，此时该字段必须是同层最后一个键。

**提交语义**

- 流式处理产生的副作用建议先作为临时结果，在读到结束符后再提交。例如先写入临时文件，收到结束符后再重命名为目标文件。
- 如果流在结束符之前中断，消息被判定为截断，临时结果应当丢弃。

## 8. 不可注入性与已知限制

由符合规范的编码器写出的内容，不可能被解析成结构。

**论证**

1. 编码器把内容的每一行都写成“`|` + 原文”（另加排版缩进）。
2. 解析器只根据环境和行首字符判定行的种类：以 `|` 开头的行只可能是文本行，结构行则另受缩进约束。
3. 内容只出现在 `|` 之后，无法影响这一行的行首字符，所以无法改变行的种类。
4. 内容中的空行被编码为单独的 `|`，因此内容永远不会产生空白行。
5. 结束符 `--LLF-END` 也写不出来：编码出的文本行总是以 `|` 开头，去掉前缀后不是裸的结束符 token，所以内容无法提前结束消息。
6. 因此在严格模式下，对任意字符串 s 都有 decode(encode(s)) = s。默认的宽松模式会把 CRLF 折成 LF，行尾带 CR 的内容不在此保证之内。实现应当对这两种模式分别用模糊测试验证。

注意这条保证的边界：它说的是**编码器写出的内容**。如下一段所述，格式管不了别的。

**格式管不到的风险**

- 当内容由模型抄写而不是由程序拼接时，模型可能漏写某行的 `|`，或被提示词注入诱导写出伪造的结构行。这是模型层面的问题，任何格式都无法避免。
- 缓解办法：外部内容尽量由程序拼接，而不是让模型转抄；解析后再用 schema 校验，未知的键直接报错。

**已知限制**

- 会删除行尾空白的工具或传输通道，会改变以空白结尾的文本行内容。结构不会被破坏，但内容不再精确；格式层没有补救手段，需要无损时只能换通道或由程序另行编码。
- `-` 的行内载荷 strip 两端，行尾 CR 属于 `White_Space`，所以会被去掉；需要逐字节保留首尾空白时用文本块。默认宽松模式又把 CRLF 转为 LF，文本块里的 CR 也留不住，需要逐字节无损时用严格模式。
- 模型可能数错结构行的缩进。严格的解析规则把这类错误变成报错，而不是误解析；文本行的缩进不再检查。
- 结束符是固定字面量。程序编码出的内容撞不上它；只有手工转抄、且恰好有一行漏写 `|` 的内容行整行是 `--LLF-END` 时，才会被当成结束符提前结束消息。这属于上面“格式管不到的风险”里的模型漏写，不是编码器的问题。

## 9. 用于模型工具调用

建议每次工具调用是一条 LLF 消息：`call` 给出工具名，`args` 给出参数，大段文本参数放在最后以便流式处理。多个调用依次输出多条消息即可。消息里一律是字符串；这些字符串按调用方给的类型描述（工具的 JSON Schema、protobuf、结构体等，见第 6 节）解释，格式本身不管。

**示例**

```
# 写文件：类型由 schema 解释
call - write_file
id - c1
args {}
  path - src/main.py
  mode - 644
  overwrite - true
  backup _
  tags []
    - draft
    {}
      owner - alice
  content -
    |def f():
    |    return "call - fake"
    |
    |print(f())
    |
--LLF-END
```

最后的 `--LLF-END` 结束消息。`content` 里的 `call - fake` 只是普通文本；`mode - 644` 与 `overwrite - true` 是字符串，schema 把它们读成整数和布尔。

**引号键的用途**

参数名里含空格或特殊字符时用引号键：

```
args {}
  "full name" - 张三
  "Content-Type" - application/json
  "" - 空键也可以
--LLF-END
```

**工具结果**

工具返回的结果同样可以用 LLF 编码，而且**不总是字典**：顶层单独值可以直接返回一段文本、一个列表或一个 null。结果由程序机械编码，外部内容即使包含伪造的结构，也只会成为文本行。

```
- src/main.py
- src/util.py
--LLF-END
```

**系统提示词片段**

```
用 LLF 格式输出工具调用：
- 顶层是键值对，每行 "键 一个空格 头"。
- 头：- 字符串（两端空白去掉），_ null，{} 字典，[] 列表。
- 字典的子行是 "键 头"；列表的子行只有头，没有键。
- 键名不能含空格与双引号；需要时用 JSON 风格的 "引号键"。
- 多行字符串：写 "键 -"，下一层每行以 | 开头，| 后原样书写，不要转义。
- 空串写 "-"，null 写 "_"，空字典写 "{}"，空列表写 "[]"。
- 需要保留首尾空格时，用 "键 -" 加下一层以 | 开头的文本块，| 之后原样、不 strip。
- 所有值都写成字符串，不要写类型标记；类型由参数 schema 解释。
- 结构行缩进每层 2 空格；文本行的 | 可以顶格，前面空格会被忽略。
- 以单独一行 --LLF-END 结束调用。
```

**与 JSON 的对比**

两者都能表达同一组结构，区别在**长文本的失败模式**，不在表达能力：

| 方面 | JSON | LLF |
| --- | --- | --- |
| 长文本 | 引号、反斜杠、换行都要转义，转义出错会静默改变内容 | 原样书写，行前缀保证内容变不成结构 |
| 转义成本 | 编码器必须识别并处理特殊字符 | 编码器对任意内容都是同一套算法（只有键名可能要用引号） |
| 流式 | 需要增量解析器 | 逐行天然可解析 |
| 截断检测 | 括号不闭合时可检测 | 缺少结束符即可检测 |
| 类型 | 字面形式带类型（number/bool/null 各自成型） | 只有字符串，类型由外部类型描述（如 JSON Schema）解释 |
| 生态 | 成熟 | 需要新实现 |

建议用实测数据支撑推广：在长代码参数的工具调用上，对比 JSON 与 LLF 的解析失败率、内容错误率和 token 数。

## 10. 与 JSON 的映射（可选）

LLF 的结构与 JSON 一一对应，差别只在标量：LLF 没有数字与布尔的字面形式，它们都是字符串，由 schema 解释。这一节给出一种最常用的落地方式，**不是格式的要求**：LLF 可以直接映射到 protobuf、语言结构体或自家类型，不必经过 JSON。

**LLF 到 JSON（无 schema 时）**

| LLF | JSON |
| --- | --- |
| `- 载荷` | string |
| `_` | null |
| `{}` | object，保留键顺序 |
| `[]` | array |

有 schema 时，先按 JSON Schema 把每个 `-` 字符串解释成声明类型（integer、number、boolean 等），再转 JSON。

**JSON 到 LLF**

- null → `_`。
- string → `- 载荷`；含 LF 或首尾有空白（`White_Space`）的用文本块。
- array → `[]`，元素依次写成项行。
- object → 隐式键值对（顶层）或 `{}`（嵌套）。
- number / bool → `- 文本`（如 `- 3`、`- true`）；类型由 JSON Schema 恢复。
- 顶层是 object → 隐式键值对；是其他任意值（array、string、number、bool、null）→ 顶层单独值。这是顶层单独值必须保留的原因，也是工具结果不一定是字典时的落点。
- 键名：普通键与引号键合起来可以表示任意 JSON 键（含空键、含空格、含双引号、含换行、恰好是四个头名）。
- **单独看一份 LLF 而没有 schema 时，number / string 的区别无法从格式里恢复**；但内容不丢，接上 schema 即可还原。这是把类型交给 schema 的必然结果，不是缺陷。

## 11. 错误情况与测试向量

解析器遇到以下任何情况都必须报错，禁止容错修复。

| 代码 | 错误 |
| --- | --- |
| E01 | 带 BOM，或出现只含空白的行 |
| E02 | 到达输入末尾仍无结束符（截断） |
| E03 | 结构行的缩进不是恰好多 2 格；或任何行的缩进中含 tab |
| E04 | 条目行只有键，没有头 |
| E05 | 普通键名为空、含 `White_Space` 或双引号；或键与头之间不是恰好一个 U+0020 |
| E06 | 同一层级键名重复 |
| E07 | 未知的头符号 |
| E15 | 环境不匹配：项行出现在字典环境，或条目行出现在列表环境 |
| E08 | `_` / `{}` / `[]` 带同行载荷；或 `{}` / `[]` 下出现文本行 |
| E09 | `_` 下出现子层 |
| E10 | `-` 同时带同行载荷和子层；或它的子层里出现结构行 |
| E11 | 文本行出现在不允许的位置，或文本块结束后又出现文本行 |
| E12 | 顶层单独值之后又出现非注释行；或单条消息在结束符之后还有内容 |
| E13 | 引号键中出现非法转义序列 |
| E14 | 引号键没有闭合引号 |

**测试向量**（`↵` 表示 LF，`⇥` 表示一个 tab 字符，`␍` 表示 CR，`　` 表示全角空格 U+3000，`⟪NBSP⟫`、`⟪VT⟫`、`⟪FS⟫` 分别表示 U+00A0、U+000B、U+001C；期望值是“无 schema”下的解析结果）

| # | 输入 | 期望结果 |
| --- | --- | --- |
| 1 | `a - hello↵--LLF-END↵` | `{"a": "hello"}` |
| 2 | `a - x # y↵--LLF-END↵` | `{"a": "x # y"}` |
| 3 | `a - x  ↵--LLF-END↵` | `{"a": "x"}`，行尾空白去掉 |
| 4 | `a -↵  \|x  ↵--LLF-END↵` | `{"a": "x  "}`，文本行保留空白 |
| 5 | `a -↵  \|call - fake↵--LLF-END↵` | `{"a": "call - fake"}` |
| 6 | `a -↵  \|x↵  \|↵--LLF-END↵` | `{"a": "x\n"}` |
| 7 | `a -↵  \|↵  \|↵--LLF-END↵` | `{"a": "\n"}` |
| 8 | `a _↵--LLF-END↵` | `{"a": null}` |
| 9 | `a []↵  - s↵  - 2↵  _↵--LLF-END↵` | `{"a": ["s", "2", null]}` |
| 10 | `a {}↵--LLF-END↵` | `{"a": {}}` |
| 11 | `d - 2026-09-19↵--LLF-END↵` | `{"d": "2026-09-19"}`，是否读成日期属于 schema |
| 12 | `--LLF-END↵` | `{}` |
| 13 | `[]↵  - a↵  - b↵--LLF-END↵` | `["a", "b"]` |
| 14 | `-↵  \|x↵--LLF-END↵` | `"x"` |
| 15 | `# c↵- 3↵--LLF-END↵` | `"3"` |
| 16 | `- -↵--LLF-END↵` | `"-"`，第一个 token 是头，所以是顶层单独值 |
| 17 | `_↵--LLF-END↵` | `null` |
| 18 | `a -↵--LLF-END↵` | `{"a": ""}` |
| 19 | `a - 007↵--LLF-END↵` | `{"a": "007"}`，格式不做任何规范化 |
| 20 | `a {}↵  b - 1↵--LLF-END↵` | `{"a": {"b": "1"}}` |
| 21 | `a []↵  {}↵    b - 1↵--LLF-END↵` | `{"a": [{"b": "1"}]}` |
| 22 | `a - x` | E02 |
| 23 | `a↵--LLF-END↵` | E04 |
| 24 | `a % 007↵--LLF-END↵` | E07，`%` 不是头 |
| 25 | `a - x↵a - y↵--LLF-END↵` | E06 |
| 26 | `a {}↵   b - 1↵--LLF-END↵` | E03，结构行缩进 3 格 |
| 27 | `a -↵  \|x↵  # c↵  \|y↵--LLF-END↵` | E11 |
| 28 | `- 3↵a - x↵--LLF-END↵` | E12 |
| 29 | `a {}↵  - - v↵--LLF-END↵` | E15，第一个 token 是头，按项行归类 |
| 30 | `a _↵  b - 1↵--LLF-END↵` | E09 |
| 31 | `a []↵  - x↵  b - 1↵--LLF-END↵` | E15 |
| 32 | `a -↵  \|# 不是注释↵--LLF-END↵` | `{"a": "# 不是注释"}` |
| 33 | `d @date 2026-09-19↵--LLF-END↵` | E07，`@date` 不是头 |
| 34 | `名字 - 柚子↵--LLF-END↵` | `{"名字": "柚子"}`，允许非 ASCII 键名 |
| 35 | `@x - v↵--LLF-END↵` | `{"@x": "v"}`，`@` 只是普通字符 |
| 36 | `a#b - v↵--LLF-END↵` | `{"a#b": "v"}` |
| 37 | `"a b" - v↵--LLF-END↵` | `{"a b": "v"}`，引号键含空格 |
| 38 | `"" - v↵--LLF-END↵` | `{"": "v"}`，空键 |
| 39 | `"-" - v↵--LLF-END↵` | `{"-": "v"}`，四个头名之一也能用引号键写 |
| 40 | `"a\"b" - v↵--LLF-END↵` | `{"a\"b": "v"}`，键是 `a"b` |
| 41 | `"a\tb" - v↵--LLF-END↵` | `{"a\tb": "v"}`，键含 tab |
| 42 | `"#foo" - v↵--LLF-END↵` | `{"#foo": "v"}` |
| 43 | `"a\x" - v↵--LLF-END↵` | E13 |
| 44 | `"a - v↵--LLF-END↵` | E14 |
| 45 | `a"b - v↵--LLF-END↵` | E05，普通键名不能含双引号 |
| 46 | `a⇥b - v↵--LLF-END↵` | E05，普通键名不能含 tab |
| 47 | `a - 1↵--LLF-END↵a - 2↵--LLF-END↵` | 两条消息，依次为 `{"a": "1"}` 和 `{"a": "2"}`，不得粘连 |
| 48 | `"a" - 1↵a - 2↵--LLF-END↵` | E06，解引号后键名相同 |
| 49 | `a - ↵--LLF-END↵` | `{"a": ""}`，头后有一个空格，其余为空 |
| 50 | `a {}↵  b []↵    - x↵c - y↵--LLF-END↵` | `{"a": {"b": ["x"]}, "c": "y"}`，一次退回顶层 |
| 51 | `{}↵  a - 1↵--LLF-END↵` | `{"a": "1"}`，顶层单独值写成显式字典 |
| 52 | `a - x↵b - y↵--LLF-END↵` | `{"a": "x", "b": "y"}`，键顺序保留 |
| 53 | `a {}↵  \|x↵--LLF-END↵` | E08，`{}` 下出现文本行 |
| 54 | `a []↵  \|x↵--LLF-END↵` | E08，`[]` 下出现文本行 |
| 55 | `\|x↵--LLF-END↵` | E11，顶层文本行 |
| 56 | `a -↵  \|  x  ↵--LLF-END↵` | `{"a": "  x  "}`，文本块保留首尾空白 |
| 57 | `"\|" - v↵--LLF-END↵` | `{"\|": "v"}`，以 `\|` 为名的键用引号键 |
| 58 | `a - 　x　↵--LLF-END↵` | `{"a": "x"}`，全角空格属于 `White_Space`，被 strip |
| 59 | `a -↵\|x↵--LLF-END↵` | `{"a": "x"}`，文本行顶格 |
| 60 | `a -↵      \|x↵--LLF-END↵` | `{"a": "x"}`，文本行任意缩进 |
| 61 | `a {}↵  b -↵\|x↵  c - y↵--LLF-END↵` | `{"a": {"b": "x", "c": "y"}}`，顶格文本行之后回到结构层 |
| 62 | `a - v↵  --LLF-END  ↵` | `{"a": "v"}`，结束符两侧空白忽略 |
| 63 | `a - v↵--LLF-END`（无末尾 LF） | `{"a": "v"}` |
| 64 | `a -↵  \|--LLF-END↵--LLF-END↵` | `{"a": "--LLF-END"}`，\| 之后可以写结束符字面量 |
| 65 | `--LLF-END - v↵--LLF-END↵` | `{"--LLF-END": "v"}`，结束符字面量可以作普通键 |
| 66 | `a -↵  \|x↵↵--LLF-END↵` | E01，消息体里的空行不是内容 |
| 67 | `a - v↵--LLF-END↵x - y↵` | E12，单条消息在结束符之后不得有内容 |
| 68 | `a -↵  \|x↵` | E02，文本块被截断 |
| 69 | `--LLF-END↵--LLF-END↵` | 两条消息，依次为 `{}` 和 `{}` |
| 70 | `a - v↵↵` | E02，旧的空行结束符不再有效 |
| 71 | `a -↵⇥\|x↵--LLF-END↵` | E03，缩进中含 tab |
| 72 | `↵` | E02，单个 LF 不再表示空消息 |
| 73 | `　 - v↵--LLF-END↵` | E05，普通键名不能含 `White_Space` |
| 74 | `"　" - v↵--LLF-END↵` | `{"　": "v"}`，含空白的键用引号键 |
| 75 | `k⟪NBSP⟫ - v↵--LLF-END↵` | E05，NBSP 属于 `White_Space` |
| 76 | `"k⟪NBSP⟫" - v↵--LLF-END↵` | `{"k⟪NBSP⟫": "v"}` |
| 77 | `k⟪VT⟫ - v↵--LLF-END↵` | E05，VT（U+000B）属于 `White_Space` |
| 78 | `"k⟪VT⟫" - v↵--LLF-END↵` | `{"k⟪VT⟫": "v"}` |
| 79 | `k⟪FS⟫ - v↵--LLF-END↵` | `{"k⟪FS⟫": "v"}`，U+001C 不是 `White_Space`，可作普通键 |

**严格模式用例**（`␍` 表示 CR，`↵` 表示 LF；宽松模式下不适用）

| # | 输入 | 期望结果 |
| --- | --- | --- |
| S1 | `a -↵  \|x␍↵--LLF-END↵` | `{"a": "x\r"}`，严格模式保留 CR |
| S2 | `a - x␍↵--LLF-END↵` | `{"a": "x"}`，CR 属于行尾空白，被去掉 |

机器可读的测试向量见 [vectors.json](vectors.json)（由 [tools/build_vectors.py](tools/build_vectors.py) 生成），测试见 [tests/test_llf.py](tests/test_llf.py)。

## 12. 命名与版本变化

**为什么改名（v0.4）**

格式不做任何类型解释，类型已经完全交给 schema。旧名 `Line-Typed Format` 里的 “Typed” 会让每个新读者以为值带类型，和格式的主张相反。新名 **LLF = Literal Line Format（字面行格式）** 落在两个删不掉的性质上：**零转义（literal）** 与 **行结构（line）**。扩展名 `.llf`，MIME `application/llf`（见第 13 节）。

**v0.1 → v0.2**

- 删除标量类型符号 `~` `%` `/` `?`：它们不再是合法头，出现即 E07。
- `_` 从类型符号改成头，表示 null 值。
- `-` 成为标量头。
- 行的种类由父头所处的环境决定：`{}` 的子层是条目行，`[]` 的子层是项行。
- 键名不能恰好是 `-` 或 `_`。
- 标量类型交给 schema；格式不再解析整数、浮点、布尔。

**v0.2 → v0.3**

- 删除逃生口 `@name`：所有标量本来就是字符串，类型解释已经在 schema 侧。
- 数据模型明确为字典、列表、字符串、null。
- 文本行只出现在 `-` 之下；头收敛为四个。

**v0.3 → v0.4**

- 改名 LTF → LLF（见上）。
- 键名允许任意非 ASCII 字符。
- 明确不规定嵌套深度与消息大小的上限。
- 明确顶层单独值必须保留。
- 明确 null 允许，由 `_` 表示。
- 明确不定义 schema 语言，直接复用 JSON Schema。
- 明确无 schema 时值一律为字符串，信息不丢。

**v0.4 → v0.5**

- 新增引号键逃生口：JSON 风格的 `"…"` 加常规转义。含空格、tab、双引号、换行、空字符串，以及恰好是四个头名的键，现在都能表示。
- JSON 键映射的最后一个缺口补上。
- 宽松模式（CRLF→LF）改为默认；严格模式用于逐字节无损往返。
- 键名按码点序列比较，不做 Unicode 归一化；建议生成方输出 NFC。
- 与 NestedText 的差异改述为「字典与列表的表示方式」：LLF 显式写 `{}` / `[]`，嵌套与空容器无歧义。

**v0.5 → v0.6**

- 加入参考实现：`llf.py`（解析器 + 编码器）、机器可读测试向量 `vectors.json`、测试 `tests/test_llf.py`（46 条向量 + 2000 组往返 fuzz）。
- 不改变格式语义。

**v0.6 → v0.7**

- 键名重复判定发生在解引号之后。
- 不可注入性的往返保证限定于严格模式。
- E07 收窄为「未知的头符号」，新增 E15「环境不匹配」。
- 新增测试向量 47–52 与严格模式用例 S1–S2。
- `-` 的内联载荷把行尾 CR 也当行尾空白去掉（S2 要求）。
- 暂定 MIME 由 `text/llf` 改为 `application/llf`，理由见第 13 节。

**v0.7 → v0.8**

- 新增行内头 `|`：逐字节字符串，不 strip，禁止子层。`|` 在格式中的含义统一为“从此处原样”。
- 头由四个增为五个，相关措辞与键名规则同步。
- `-` 的 strip 范围放宽为 Unicode `White_Space` 属性的全部码点，两端都去，按属性定义而非按某语言库定义。
- 明确行的归类先于键名校验：第一个 token 是头即按项行处理。E15 的两个子句因此都可达，向量 29 由 E05 改为 E15。
- 编码器的形式选择规则：含 LF 用文本块，首尾有空白用行内 `|`，其余用 `-`。
- 新增测试向量 53–62。
- E08 删去已不可达的「`{}` / `[]` 下出现文本行」：`|` 成为头之后，该位置要么是项行（字典环境 E15），要么在列表里合法。
- `|` 行落在更深的位置仍按 E11（遗留文本行）处理，向量 27 不变。

**v0.8 → v0.9**

- 撤回行内头 `|`：它在列表环境里与文本行前缀同形，列表项因此混淆。头仍为四个。
- 保留 v0.8 中与 `|` 无关的两项：`-` 的行内载荷按 Unicode `White_Space` 两端 strip；行的归类先于键名校验。
- 编码器选形：含 LF 或首尾有空白 → 文本块（可只有一行）；其余 → 行内 `-`。
- E08 恢复第二子句；E10 去掉 `|` 子句。
- 普通键名不能以 `|` 开头（那是文本行），这类键用引号键。
- 测试向量 53–62 替换为 53–58。

**v0.9 → v0.10**

- 文本行不再要求“比 `-` 所在行多 2 格”：`|` 之前的空格一律忽略，文本行可以顶格或任意缩进。编码器仍按父行 +2 格排版，解码器不检查文本行缩进。
- 结束符由末尾空行改为单独一行 `--LLF-END`（去掉首尾 `White_Space` 后整行匹配）。它是可见字面 token，不会被删除行尾空白的通道弄丢；单条消息在结束符之后不得再有内容（E12）。
- 消息体里的空行仍是 E01，不作为内容；文本块仍在第一个非 `|` 行（含结束符行）结束。
- E02 改为“没有 `--LLF-END`”；E03 的“缩进不是恰好多 2 格”收窄到结构行，任何行缩进含 tab 仍报 E03。
- 测试向量扩到 72 条，新增 59–72：顶格/任意缩进的文本行、结束符两侧空白与无末尾 LF、结束符字面量作内容或键，以及旧空行结束符（含单个 LF）不再有效。

**v0.10 → v0.11**

- 修正普通键名的空白判断：普通键名不得含任何 `White_Space` 字符（NBSP、全角空格 U+3000、VT、FF 等）；编码器对这类键改用引号键，解码器对未加引号的这类键报 E05。
- 明确行内分词只认 U+0020：头符号识别与普通键名提取不再使用语言自带的 `split(None)`。此前以 NBSP、U+3000 等开头的普通键会被误判成头，例如 `dumps({"\u3000": "v"})` 解不回来。
- 新增测试向量 73–79。
- 文档澄清（不改格式）：新增第 6 节“格式与 schema 解耦”，写明 LLF 不依赖 JSON / JSON Schema、不定义词法，用哪种 schema 由调用方自选；修正 v0.10 后残留的“`-` 的子层只能是文本行”措辞，改为“紧跟 `-` 的连续 `|` 行”；明确单条消息入口与消息流入口是**调用层**语境。测试加入 5000 组随机字节 fuzz。
- 新增参考语法 `grammars/llf.gbnf`（GBNF，供 llama.cpp / XGrammar）与 `grammars/llf.lark`（Lark，供 outlines），由同一个 `tools/build_grammars.py` 生成，`tools/check_grammar.py` / `tools/check_lark.py` 校验。
- 定位澄清：类型解释明确为**格式之外的扩展**。第 1 节只保留“字典/列表/字符串/null，除 `_` 外标量全是字符串”；第 6 节改为“非规范性附录”，第 10 节标注“可选”。

## 13. 待定问题

- [ ] MIME 与扩展名：目标 `application/llf`（无前缀属标准树，需走 6838bis 的 community format 或 RFC）；过渡可先注册 `application/prs.llf`（个人树）。`.llf` 先去 mime-db / shared-mime-info，再考虑 IANA。
- [ ] 消息流中两条消息之间是否允许空行（当前不允许；手写与日志拼接场景略紧）。
- [ ] 端到端示例：把 `grammars/llf.gbnf` / `grammars/llf.lark` 接进 llama.cpp / outlines 的完整调用样例。语法本身已提供并验证。
- [ ] 是否有其它语言需要参考实现（当前只有 Python）。
