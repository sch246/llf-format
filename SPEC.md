# LTF 行式类型格式规范 v0.1 草案

2026-09-19

## 1. 概述与设计目标

LTF（Line-Typed Format，暂名）是一种以行为单位、显式标注类型的数据格式，主要面向大模型的工具调用，也适合手写。它的核心性质是：内容无需任何转义，而任何内容都不可能在格式上伪装成结构。

设计目标：

- **零转义**：字符串内容原样书写，引号、反斜杠、括号、任意符号都不需要处理。
- **格式上不可注入**：内容行永远带有固定前缀 `|`，只有结构行能出现在结构位置。
- **显式类型**：每个值由类型符号开头，解析器从不根据值的样子猜测类型。
- **可流式**：每一行的含义只由其自身的缩进和首字符决定，不需要向后看；多行字符串可以边生成边消费。
- **可检测截断**：消息必须以结束符结束，流中途断开会被识别为不完整。
- **适合手写**：没有引号、括号配对和转义，结构靠缩进。

本规范中，“必须”“禁止”表示强制要求，“建议”表示推荐做法。

## 2. 基本约定

一条 LTF 消息由若干行组成，以结束符结束。顶层可以是隐式键值对，也可以是一个单独的值。

**编码**

- 文本必须是 UTF-8，禁止带 BOM。

**行**

- 行只以 LF（`\n`）分隔。CR（`\r`）是普通字符，属于行内容。
- 严格模式下不做任何换行符转换，从而保证任意字符串都能无损往返。
- 实现可以提供宽松模式，在解析前把 CRLF 转为 LF，便于手写。宽松模式下，行尾带 CR 的内容无法表示。

**缩进**

- 每层缩进恰好 2 个空格，顶层为 0 格。
- 缩进中禁止出现 tab。子行必须恰好比父行多 2 格，多或少都是错误。

**消息结束**

- 结束符是连续两个 LF（`\n\n`），即上一行的换行后紧跟一个空行。解析器读到第一个 LF 时只能确定当前行结束，必须看到下一个字符才能判断：若是 LF 则消息结束，否则是下一行的开始。任何实现都禁止在第一个 LF 处就判定消息结束，否则流式生成时会误截断。
- 只含空白字符的行是错误，不是结束符。
- 到达输入末尾（EOF）却没有遇到结束符，消息视为被截断，必须报错。
- 一个流中可以依次传输多条消息，每条以结束符结束。
- 只有一个空行的消息，表示空的顶层键值对。

**顶层形态**

- 第一个非注释行决定整条消息的形态：以键开头，顶层是隐式键值对；以类型符号开头，顶层是一个单独的值。
- 顶层单独值的写法与列表项行相同，子层缩进仍为 2 格。
- 顶层单独值只能有一个。之后再出现任何非注释行，都是错误。
- 由于键名字符集不包含类型符号，第一行即可确定形态，流式解析不需要向后看。
- 工具调用建议使用键值对形态；单独值适合工具结果，比如直接返回一段文本或一个列表。

```
[]
  ~ src/main.py
  ~ src/util.py

```

## 3. 行的种类与键名

除空行外，每一行都属于以下四种之一，种类由所在位置和行首字符唯一确定。

| 行的种类 | 形式 | 出现位置 |
| --- | --- | --- |
| 条目行 | 缩进 + 键 + 空格 + 值头 | 顶层，或 `{}` 的子层 |
| 列表项行 | 缩进 + 值头 | `[]` 的子层，或顶层单独值 |
| 文本行 | 缩进 + `\|` + 内容 | `~` 或 `@name` 的子层 |
| 注释行 | 任意空格 + `#` + 任意内容 | 除文本块内部以外的任何位置 |

**值头**

值头 = 类型符号，后面可选“一个空格 + 单行载荷”。类型符号见第 4 节。

**键名**

- 键名只能由 `A-Z a-z 0-9 _ . -` 组成，至少一个字符，区分大小写。
- 键名和类型符号之间恰好一个空格。
- 同一层级内键名禁止重复，重复即报错。
- 键的书写顺序必须保留。
- 条目行只有键、没有类型符号，是错误。null 必须显式写 `_`。

**注释**

- 以 `#` 开头（前面可以有任意空格）的行是注释，整行忽略，不参与缩进校验。
- 注释只能独占一行，没有行尾注释。`key ~ a # b` 的值就是 `a # b`。
- 注释禁止出现在文本块中间，原因见第 5 节。

由于键名字符集不包含任何类型符号、`|` 和 `#`，四种行在任何位置都不会混淆。

## 4. 类型符号

每个值都以类型符号开头，类型由书写者声明，解析器从不猜测。

| 符号 | 类型 | 同行载荷 | 子层 | 什么都不写时 |
| --- | --- | --- | --- | --- |
| `~` | 字符串 | 可选，单行文本 | 可选，文本行 | 空字符串 |
| `%` | 整数 | 必须 | 禁止 | 错误 |
| `/` | 浮点数 | 必须 | 禁止 | 错误 |
| `?` | 布尔 | 必须，`true` 或 `false` | 禁止 | 错误 |
| `_` | null | 禁止 | 禁止 | 即 null |
| `{}` | 键值对 | 禁止 | 可选，条目行 | 空键值对 |
| `[]` | 列表 | 禁止 | 可选，列表项行 | 空列表 |
| `@name` | 扩展类型 | 可选，单行文本 | 可选，文本行 | 空载荷 |

助记：`%` 取模对应整数，`/` 除法对应浮点数。

**字符串 `~`**

- `~ 文本`：符号后恰好一个空格，其余部分原样作为值，只去掉行尾的空格和 tab。
- `~` 后跟缩进的文本行：多行字符串，见第 5 节。
- 同行载荷和子层文本行不能同时出现。
- 需要保留首尾空白的值，必须用多行形式书写。

**整数 `%`**

- 语法：`0` 或 `-?[1-9][0-9]*`。禁止 `+` 号、前导零、`-0` 和数字分隔符。
- 精度不设上限。实现无法表示某个整数时必须报错，禁止静默截断。

**浮点数 `/`**

- 语法：`-?(0|[1-9][0-9]*)(\.[0-9]+)?([eE][+-]?[0-9]+)?`，例如 `/ 3`、`/ -0.5`、`/ 1e-9`。
- v0.1 不支持 `inf` 和 `nan`，需要时可用扩展类型表示。

**键值对 `{}` 与列表 `[]`**

- 符号后不能有载荷，子层每项递归遵循本规范。
- 列表项可以混合不同类型。

**扩展类型 `@name`**

- 名称字符集与键名相同，例如 `@date`、`@base64`、`@uuid`。
- 载荷一律是字符串，写法与 `~` 完全相同，可单行也可多行。
- 解析器遇到不认识的扩展类型，必须原样返回“类型名 + 载荷字符串”，交给上层处理，不得报错。这样新增类型不会破坏旧解析器。

## 5. 多行字符串

多行字符串是 `~` 或 `@name` 下方连续的文本行，每行以 `|` 开头，`|` 之后直到行尾的全部字符原样保留。

**规则**

- 文本行的缩进必须恰好比 `~` 所在行多 2 格。
- 每行内容 = `|` 之后的全部字符，包括首尾空格、tab、`|`、`#` 等，不做任何处理。
- 单独一个 `|` 表示一个空内容行。
- 值 = 所有内容行用 LF 连接，末尾不附加换行。
- 需要末尾换行时，在最后多写一行单独的 `|`。
- 文本块在遇到第一个非文本行（条目行、列表项行、注释行或空行）时立即结束。之后再出现属于同一个值的文本行，是错误。

**为什么文本块不能被打断**

多行字符串可能包含任意内容，所以它必须是一个连续、不可分割的整体。注释或任何其他格式元素都不能插入其中，文本块从第一个 `|` 行开始，到第一个非 `|` 行结束，中间只允许 `|` 行。

这条规则还带来一个实际好处：代码里有大量以 `#` 开头的行，如果模型漏写了某行的 `|`，这一行会结束文本块，后续的 `|` 行触发 E11 报错，而不是被当成注释悄悄丢掉。

**示例**

```
script ~
  |def f():
  |    return "x ~ y" # 不是注释
  |
  |print(f())
  |
```

值为 `def f():\n    return "x ~ y" # 不是注释\n\nprint(f())\n`，末尾带一个换行。

**编码算法**

对任意字符串 s：按 LF 切分为若干行，每行写成“缩进 + `|` + 该行”。这个算法对所有字符串都有效，不需要检查内容。空字符串建议直接写 `~`。

## 6. 流式生成与解析

LTF 的每一行只凭自身的缩进和行首字符就能确定含义，因此解析器可以逐行、甚至逐字符地输出结果，不需要缓冲整条消息。

**逐行**

解析器每读完一行就产生一个事件，例如：进入键值对、键 + 标量值、开始字符串、文本行、结束当前层级、消息结束。上层可以随事件立即行动。

**逐字符**

- 一个文本行一旦读到行首的 `|`，其后的每个字符都可以立即交给上层，直到 LF。行内没有任何字符能改变已输出内容的含义。
- 单行 `~` 值要去掉行尾空白，所以末尾的空格和 tab 需要暂存到 LF 出现为止。

**字段顺序**

- 建议把大段文本字段放在消息最后。这样接收方先拿到其余参数，比如先得到文件路径并打开文件，再边接收边写入内容。
- 工具的 schema 可以把某个字段声明为“流式字段”，此时该字段必须是同层最后一个键。

**提交语义**

- 流式处理产生的副作用建议先作为临时结果，在读到结束符后再提交。例如先写入临时文件，收到结束符后再重命名为目标文件。
- 如果流在结束符之前中断，消息被判定为截断，临时结果应当丢弃。

## 7. 不可注入性与已知限制

在严格模式下，由编码器写出的任意字符串内容，都不可能被解析成结构。

**论证**

1. 编码器把内容的每一行都写成“固定缩进 + `|` + 原文”。
2. 解析器只根据缩进和行首字符判定行的种类。位于该缩进、以 `|` 开头的行，只可能是文本行。
3. 内容只出现在 `|` 之后，无法影响这一行的缩进和行首字符，所以无法改变行的种类。
4. 内容中的空行被编码为单独的 `|`，因此内容永远不会产生空行，也就无法提前结束消息。
5. 因此对任意字符串 s，都有 decode(encode(s)) = s。实现应当用模糊测试验证这一性质。

这与转义的区别在于：前缀对每一行都相同，与内容无关，编码器不需要识别任何特殊字符。

**格式管不到的风险**

- 当内容由模型抄写而不是由程序拼接时，模型可能漏写某行的 `|`，或被提示词注入诱导写出伪造的结构行。这是模型层面的问题，任何格式都无法避免。
- 缓解办法：外部内容尽量由程序拼接，而不是让模型转抄；解析后再用 schema 校验，未知的键直接报错。

**已知限制**

- 会删除行尾空白的工具或传输通道，会改变以空白结尾的文本行内容。结构不会被破坏，但内容不再精确。对精确度要求高的内容，可以改用 `@base64`。
- 宽松模式把 CRLF 转为 LF，因此无法表示行尾带 CR 的内容。
- 模型可能数错缩进。严格的解析规则把这类错误变成报错，而不是误解析。

## 8. 用于模型工具调用

建议每次工具调用是一条 LTF 消息：`call` 给出工具名，`args` 给出参数，大段文本参数放在最后以便流式处理。多个调用依次输出多条消息即可。

**示例**

```
# 写文件
call ~ write_file
id ~ c1
args {}
  path ~ src/main.py
  mode % 644
  overwrite ? true
  backup _
  created @date 2026-09-19
  tags []
    ~ draft
    {}
      owner ~ alice
  content ~
    |def f():
    |    return "call ~ fake"
    |
    |print(f())
    |

```

最后的空行结束消息。`content` 里的 `call ~ fake` 只是普通文本。

**工具结果**

工具返回的结果同样可以用 LTF 编码。结果由程序机械编码，外部内容即使包含伪造的结构，也只会成为文本行。

**系统提示词片段**

```
用 LTF 格式输出工具调用：
- 每行 "键 类型符号 值"，缩进每层 2 空格。
- 类型：~ 字符串，% 整数，/ 浮点，? 布尔，_ null，{} 键值对，[] 列表。
- 多行字符串：写 "键 ~"，下一层每行以 | 开头，| 后原样书写，不要转义。
- 列表项省略键，直接以类型符号开头。
- 以一个空行结束调用。
```

**与 JSON 对比**

| 方面 | JSON | LTF |
| --- | --- | --- |
| 长文本 | 需转义引号、反斜杠、换行 | 原样书写 |
| 格式注入 | 依赖转义正确 | 前缀机制保证不可能 |
| 流式 | 需增量解析器 | 逐行天然可解析 |
| 截断检测 | 括号不闭合时可检测 | 缺少结束符即可检测 |
| 类型 | 由字面形式推断 | 显式符号声明 |
| 生态 | 成熟 | 需要新实现 |

建议用实测数据支撑推广：在长代码参数的工具调用上，对比 JSON 与 LTF 的解析失败率和内容错误率。

## 9. 与 JSON 的映射

LTF 可以无损转为 JSON；反方向在键名和数字类型上有约束。

| LTF | JSON |
| --- | --- |
| `~` 字符串 | string |
| `%` 整数 | number；超出 ±2^53 时报错，或按选项转为 string |
| `/` 浮点数 | number |
| `?` 布尔 | true / false |
| `_` null | null |
| `{}` 键值对 | object，保留键顺序 |
| `[]` 列表 | array |
| `@name` 扩展 | `{"$ext": "name", "value": "载荷"}` |

**JSON 转 LTF**

- 数字：有 schema 时按 schema 决定用 `%` 还是 `/`；没有 schema 时，源文本不含小数点和指数的数字用 `%`，否则用 `/`。
- 键名：含有键名字符集以外字符的 JSON 键，v0.1 无法表示，转换时必须报错。
- 字符串：一律可以表示。含 LF 或首尾空白的字符串用多行形式。
- 顶层：JSON 顶层为 object 时转为隐式键值对，为其他任意值时转为顶层单独值。

## 10. 错误情况与测试向量

解析器遇到以下任何情况都必须报错，禁止容错修复。

| 代码 | 错误 |
| --- | --- |
| E01 | 带 BOM，或出现只含空白的行 |
| E02 | 到达输入末尾仍无结束符（截断） |
| E03 | 缩进不是恰好多 2 格，或缩进中含 tab |
| E04 | 条目行只有键，没有类型符号 |
| E05 | 键名含非法字符，或键与符号之间不是恰好一个空格 |
| E06 | 同一层级键名重复 |
| E07 | 未知的类型符号 |
| E08 | `{}` `[]` `_` 后带载荷，或 `%` `/` `?` 缺少载荷 |
| E09 | 整数、浮点数或布尔的载荷语法不合法 |
| E10 | `~` 或 `@name` 同时带同行载荷和子层文本行 |
| E11 | 文本行出现在不允许的位置，或文本块结束后又出现 |
| E12 | `%` `/` `?` `_` 下方出现子层 |
| E13 | 顶层单独值之后又出现非注释行 |

**测试向量**（`↵` 表示 LF）

| # | 输入 | 期望结果 |
| --- | --- | --- |
| 1 | `a ~ hello↵↵` | `{"a": "hello"}` |
| 2 | `a ~ x # y↵↵` | `{"a": "x # y"}` |
| 3 | `a ~ x  ↵↵` | `{"a": "x"}`，行尾空白去掉 |
| 4 | `a ~↵  \|x  ↵↵` | `{"a": "x  "}`，文本行保留空白 |
| 5 | `a ~↵  \|call ~ fake↵↵` | `{"a": "call ~ fake"}` |
| 6 | `a ~↵  \|x↵  \|↵↵` | `{"a": "x\n"}` |
| 7 | `a ~↵  \|↵  \|↵↵` | `{"a": "\n"}` |
| 8 | `a / 3↵↵` | `{"a": 3.0}` |
| 9 | `a []↵  ~ s↵  % 2↵  _↵↵` | `{"a": ["s", 2, null]}` |
| 10 | `a {}↵↵` | `{"a": {}}` |
| 11 | `d @date 2026-09-19↵↵` | 扩展值，类型名 `date`，载荷 `2026-09-19` |
| 12 | `↵` | `{}` |
| 13 | `[]↵  ~ a↵  ~ b↵↵` | `["a", "b"]` |
| 14 | `~↵  \|x↵↵` | `"x"` |
| 15 | `# c↵% 3↵↵` | `3` |
| 16 | `a↵↵` | E04 |
| 17 | `a ~ x` | E02 |
| 18 | `a % 007↵↵` | E09 |
| 19 | `a ~ x↵a ~ y↵↵` | E06 |
| 20 | `a ~↵   \|x↵↵` | E03 |
| 21 | `a ~↵  \|x↵  # c↵  \|y↵↵` | E11 |
| 22 | `% 3↵a ~ x↵↵` | E13 |

正式发布时，测试向量应另存为机器可读的文件，每条包含原始字节和期望的 JSON 或错误代码。

## 11. 待定问题

以下问题需要在 v1.0 之前决定。

- [ ] 正式名称，以及 MIME 类型和文件扩展名（暂定 `text/ltf`、`.ltf`）。
- [ ] 是否允许非 ASCII 键名，或提供一种能写任意键名的形式，以便完整映射 JSON。
- [ ] 浮点数是否支持 `inf`、`-inf`、`nan`。
- [ ] 是否建立官方扩展类型注册表，统一 `@date`、`@datetime`、`@base64`、`@uuid` 等的载荷格式。
- [ ] 是否规定嵌套深度和消息大小的上限，防止资源耗尽。
- [ ] 宽松模式（CRLF 转换）是否作为手写文件的默认模式。
- [ ] README 中说明与 NestedText 的关系：两者都用行前缀免除转义，LTF 增加了显式类型、流式语义和截断检测，定位于机器生成。
