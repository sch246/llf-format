# LTF 行式类型格式规范 v0.3 草案

2026-09-21

## 1. 概述与设计目标

LTF（Line-Typed Format，暂名）是一种以行为单位的数据格式，主要面向大模型的工具调用，也适合手写。

格式本身**不写类型、不做类型判断**。它存储的东西只有三样：字典（`{}`）、列表（`[]`）、字符串（`-`）；此外为 null 保留一个头 `_`。标量一律是字符串，某段字符串该被读成整数、浮点、布尔、日期还是别的什么，由使用者的 schema 决定。类型解释因此全部落在 schema 上，格式里没有任何类型标记或逃生口。

设计目标：

- **零转义**：字符串内容原样书写，引号、反斜杠、括号、任意符号都不需要处理。
- **格式上不可注入**：内容行永远带有固定前缀 `|`，只有结构行能出现在结构位置。
- **无类型**：格式只分帧，类型由 schema 解释。
- **可流式**：每一行的含义只由它所在的环境、缩进和首字符决定，不需要向后看。
- **可检测截断**：消息必须以结束符结束，流中途断开会被识别为不完整。
- **适合手写**：没有引号、括号配对和转义，结构靠缩进。

本规范中，“必须”“禁止”表示强制要求，“建议”表示推荐做法。

## 2. 基本约定

一条 LTF 消息由若干行组成，以结束符结束。顶层可以是隐式键值对，也可以是一个单独的值。

**编码**

- 文本必须是 UTF-8，禁止带 BOM。

**行**

- 行只以 LF（`\n`）分隔。CR（`\r`）是普通字符，属于行内容。
- 严格模式下不做任何换行符转换，从而保证任意字符串都能无损往返。
- 实现可以提供宽松模式，在解析前把 CRLF 转为 LF，便于手写。宽松模式下，行尾带 CR 的内容无法表示。

**缩进**

- 每层缩进恰好 2 个空格，顶层为 0 格。
- 缩进中禁止出现 tab。子行必须恰好比父行多 2 格，多或少都是错误。

**消息结束**

- 结束符是空行：最后一个内容行末尾的 LF 之后紧跟一个 LF（即 `\n\n`）。解析器读到第一个 LF 时只能确定当前行结束，必须看到下一个字符才能判断：若是 LF 则消息结束，否则是下一行的开始。任何实现都禁止在第一个 LF 处就判定消息结束，否则流式生成时会误截断。
- 只含空白字符的行是错误，不是结束符。
- 到达输入末尾（EOF）却没有遇到结束符，消息视为被截断，必须报错。
- 一个流中可以依次传输多条消息，每条以结束符结束。
- 只有一个空行的消息（单个 `\n`）表示空的顶层键值对。

**顶层形态**

- 第一个非注释行决定整条消息的形态：**它的第一个 token 是头**（`-`、`_`、`{}`、`[]`）时，顶层是一个单独的值；否则顶层是隐式键值对，该行是第一条条目行。
- 由于键名不能恰好是 `-` 或 `_`，也不包含 `{`、`[`，第一行即可确定形态，流式解析不需要向后看。
- 顶层单独值只能有一个。之后再出现任何非注释行，都是错误。
- 工具调用建议使用键值对形态；单独值适合工具结果，比如直接返回一段文本或一个列表。

```
[]
  - src/main.py
  - src/util.py

```

## 3. 行的种类与环境

除注释行外，每一行的种类由它所在的**环境**唯一决定。环境来自父级的头：

| 环境 | 子行只能是 |
| --- | --- |
| 顶层隐式键值对，或 `{}` 的子层 | 条目行（键 + 头） |
| `[]` 的子层，或顶层单独值 | 项行（只有头，没有键） |
| `-` 的子层 | 文本行（`|` 开头） |

四种行：

| 行的种类 | 形式 | 出现位置 |
| --- | --- | --- |
| 条目行 | 缩进 + 键 + 空格 + 头 | 顶层隐式键值对，或 `{}` 的子层 |
| 项行 | 缩进 + 头 | `[]` 的子层，或顶层单独值 |
| 文本行 | 缩进 + `|` + 内容 | `-` 的子层 |
| 注释行 | 任意空格 + `#` + 任意内容 | 除文本块内部以外的任何位置 |

**键名**

- 键名只能由 `A-Z a-z 0-9 _ . -` 组成，至少一个字符，区分大小写。
- **整个键名不能恰好是 `-` 或 `_`**，这两个 token 保留为头。`dry-run`、`_private`、`a.b` 都合法；恰好叫 `-` 或 `_` 的键写不出来。
- 键名和头之间恰好一个空格。
- 同一层级内键名禁止重复，重复即报错。
- 键的书写顺序必须保留。
- 条目行只有键、没有头，是错误。

**注释**

- 以 `#` 开头（前面可以有任意空格）的行是注释，整行忽略，不参与缩进校验。
- 注释只能独占一行，没有行尾注释。`key - a # b` 的值就是 `a # b`。
- 注释禁止出现在文本块中间，原因见第 5 节。

由于头只在行首占一个 token、键名不含 `{` `[` `#`，四种行在任何环境中都不会混淆。

## 4. 头与值

| 头 | 含义 | 同行载荷 | 子层 | 什么都不写时 |
| --- | --- | --- | --- | --- |
| `-` | 字符串 | 可选，单行文本 | 可选，文本行 | 空字符串 |
| `_` | null | 禁止 | 禁止 | 即 null |
| `{}` | 字典 | 禁止 | 可选，条目行 | 空字典 |
| `[]` | 列表 | 禁止 | 可选，项行 | 空列表 |

**规则**

- 头后接同行载荷时，恰好一个空格，其余部分原样作为值，只去掉行尾的空格和 tab。
- 同行载荷和子层文本行不能同时出现。
- `_` 不能带载荷，也不能有子层。
- `{}` 和 `[]` 不能带同行载荷，也不能有文本子层。
- 字符串不带结构子层：`-` 的子层只能是文本行。
- 需要保留首尾空白或换行的值，必须用多行形式书写。

**四种“空”**

| 写法（字典环境） | 值 |
| --- | --- |
| `key -` | 空字符串 |
| `key _` | null |
| `key {}` | 空字典 |
| `key []` | 空列表 |

在列表环境或顶层单独值里去掉键：`-`、`_`、`{}`、`[]`。

**缺省与 null 是两件事**

- 键不出现 = 缺省。
- `key _` = 显式 null。
- 键出现但没有头 = 错误，不是 null。

## 5. 多行文本

多行文本是 `-` 下方连续的文本行，每行以 `|` 开头，`|` 之后直到行尾的全部字符原样保留。

**规则**

- 文本行的缩进必须恰好比 `-` 所在行多 2 格。
- 每行内容 = `|` 之后的全部字符，包括首尾空格、tab、`|`、`#` 等，不做任何处理。
- 单独一个 `|` 表示一个空内容行。
- 值 = 所有内容行用 LF 连接，末尾不附加换行。
- 需要末尾换行时，在最后多写一行单独的 `|`。
- 文本块在遇到第一个非文本行（条目行、项行、注释行或空行）时立即结束。之后再出现属于同一个值的文本行，是错误。

**为什么文本块不能被打断**

多行字符串可能包含任意内容，所以它必须是一个连续、不可分割的整体。注释或任何其他格式元素都不能插入其中，文本块从第一个 `|` 行开始，到第一个非 `|` 行结束，中间只允许 `|` 行。

这条规则还带来一个实际好处：代码里有大量以 `#` 开头的行，如果模型漏写了某行的 `|`，这一行会结束文本块，后续的 `|` 行触发 E11 报错，而不是被当成注释悄悄丢掉。

**示例**

```
script -
  |def f():
  |    return "x - y" # 不是注释
  |
  |print(f())
  |

```

值为 `def f():\n    return "x - y" # 不是注释\n\nprint(f())\n`，末尾带一个换行。

**编码算法**

对任意字符串 s：按 LF 切分为若干行，每行写成“缩进 + `|` + 该行”。这个算法对所有字符串都有效，不需要检查内容。空字符串建议直接写 `-`。

## 6. 与 schema 的关系

格式与类型是分开的，边界如下：

- 格式只产出四种东西：字典、列表、字符串、null。`-` 的载荷永远是字符串，由格式**原样**交出，不做任何转换。
- schema 是一个读取器：它决定某个字符串位置该被解释成整数、浮点、布尔、日期、枚举还是别的类型，并负责校验（必填、可空、取值范围、未知键）。
- 因为字符串到类型的解释全部在 schema 侧，格式里不需要、也不提供任何类型标记或逃生口。同一份 LTF 文本配不同 schema 可以得到不同的类型结果，这不是歧义，是设计。
- 没有 schema 时，实现应当把所有 `-` 载荷当字符串、`_` 当 null，只保证结构无损。
- schema 的解释失败（例如 `- abc` 遇上整数 schema）是 schema 层的错误，不是格式错误。

## 7. 流式生成与解析

LTF 的每一行只凭自身所处的环境、缩进和行首字符就能确定含义，因此解析器可以逐行、甚至逐字符地输出结果，不需要缓冲整条消息。

**逐行**

解析器每读完一行就产生一个事件，例如：进入字典、键 + 字符串、开始字符串、文本行、结束当前层级、消息结束。上层可以随事件立即行动。

**逐字符**

- 一个文本行一旦读到行首的 `|`，其后的每个字符都可以立即交给上层，直到 LF。行内没有任何字符能改变已输出内容的含义。
- 单行 `-` 值要去掉行尾空白，所以末尾的空格和 tab 需要暂存到 LF 出现为止。

**字段顺序**

- 建议把大段文本字段放在消息最后。这样接收方先拿到其余参数，比如先得到文件路径并打开文件，再边接收边写入内容。
- 工具的 schema 可以把某个字符串字段声明为“流式字段”，此时该字段必须是同层最后一个键。

**提交语义**

- 流式处理产生的副作用建议先作为临时结果，在读到结束符后再提交。例如先写入临时文件，收到结束符后再重命名为目标文件。
- 如果流在结束符之前中断，消息被判定为截断，临时结果应当丢弃。

## 8. 不可注入性与已知限制

由符合规范的编码器写出的内容，不可能被解析成结构。

**论证**

1. 编码器把内容的每一行都写成“固定缩进 + `|` + 原文”。
2. 解析器只根据环境、缩进和行首字符判定行的种类。位于该缩进、以 `|` 开头的行，只可能是文本行。
3. 内容只出现在 `|` 之后，无法影响这一行的缩进和行首字符，所以无法改变行的种类。
4. 内容中的空行被编码为单独的 `|`，因此内容永远不会产生空行，也就无法提前结束消息。
5. 因此对任意字符串 s，都有 decode(encode(s)) = s。实现应当用模糊测试验证这一性质。

注意这条保证的边界：它说的是**编码器写出的内容**。如下一段所述，格式管不了别的。

**格式管不到的风险**

- 当内容由模型抄写而不是由程序拼接时，模型可能漏写某行的 `|`，或被提示词注入诱导写出伪造的结构行。这是模型层面的问题，任何格式都无法避免。
- 缓解办法：外部内容尽量由程序拼接，而不是让模型转抄；解析后再用 schema 校验，未知的键直接报错。

**已知限制**

- 会删除行尾空白的工具或传输通道，会改变以空白结尾的文本行内容。结构不会被破坏，但内容不再精确；格式层没有补救手段，需要无损时只能换通道或由程序另行编码。
- 宽松模式把 CRLF 转为 LF，因此无法表示行尾带 CR 的内容。
- 模型可能数错缩进。严格的解析规则把这类错误变成报错，而不是误解析。
- 键名不能恰好是 `-` 或 `_`，也不能包含空格、`{`、`[`、`#`。含这些字符的 JSON 键在 v0.3 无法直接表示，见第 13 节。

## 9. 用于模型工具调用

建议每次工具调用是一条 LTF 消息：`call` 给出工具名，`args` 给出参数，大段文本参数放在最后以便流式处理。多个调用依次输出多条消息即可。参数的类型由工具 schema 解释，消息里一律是字符串。

**示例**

```
# 写文件：类型由 schema 解释
call - write_file
id - c1
args {}
  path - src/main.py
  mode - 644
  overwrite - true
  backup _
  tags []
    - draft
    {}
      owner - alice
  content -
    |def f():
    |    return "call - fake"
    |
    |print(f())
    |

```

最后的空行结束消息。`content` 里的 `call - fake` 只是普通文本；`mode - 644` 与 `overwrite - true` 是字符串，schema 把它们读成整数和布尔。

**工具结果**

工具返回的结果同样可以用 LTF 编码。结果由程序机械编码，外部内容即使包含伪造的结构，也只会成为文本行。

**系统提示词片段**

```
用 LTF 格式输出工具调用：
- 顶层是键值对，每行 "键 一个空格 头"。
- 头：- 字符串，_ null，{} 字典，[] 列表。
- 字典的子行是 "键 头"；列表的子行只有头，没有键。
- 多行字符串：写 "键 -"，下一层每行以 | 开头，| 后原样书写，不要转义。
- 空串写 "-"，null 写 "_"，空字典写 "{}"，空列表写 "[]"。
- 所有值都写成字符串，不要写类型标记；类型由参数 schema 解释。
- 缩进每层 2 空格。
- 以一个空行结束调用。
```

**与 JSON 的对比**

两者都能表达同一组结构，区别在**长文本的失败模式**，不在表达能力：

| 方面 | JSON | LTF |
| --- | --- | --- |
| 长文本 | 引号、反斜杠、换行都要转义，转义出错会静默改变内容 | 原样书写，行前缀保证内容变不成结构 |
| 转义成本 | 编码器必须识别并处理特殊字符 | 编码器对任意内容都是同一套算法 |
| 流式 | 需要增量解析器 | 逐行天然可解析 |
| 截断检测 | 括号不闭合时可检测 | 缺少结束符即可检测 |
| 类型 | 字面形式带类型（number/bool/null 各自成型） | 只有字符串，类型由外部 schema 解释 |
| 生态 | 成熟 | 需要新实现 |

建议用实测数据支撑推广：在长代码参数的工具调用上，对比 JSON 与 LTF 的解析失败率、内容错误率和 token 数。

## 10. 与 JSON 的映射

**LTF 到 JSON（无 schema 时）**

| LTF | JSON |
| --- | --- |
| `- 载荷` | string |
| `_` | null |
| `{}` | object，保留键顺序 |
| `[]` | array |

有 schema 时，先按 schema 把每个 `-` 字符串解释成对应类型（整数、浮点、布尔等），再转 JSON；`_` 仍为 null。

**JSON 到 LTF**

- null → `_`。
- string → `- 载荷`；含 LF 或首尾空白的字符串用多行形式。
- array → `[]`，元素依次写成项行。
- object → 隐式键值对（顶层）或 `{}`（嵌套）。
- number / bool → `- 文本`（如 `- 3`、`- true`）。**格式不保留 number 与 string 的区别**：要恢复类型必须有 schema；没有 schema 时它们都只是字符串。
- 键名必须符合第 3 节的键名字符集，且不能恰好是 `-` 或 `_`，否则转换必须报错。
- 顶层是 object → 隐式键值对；是其他任意值 → 顶层单独值。

## 11. 错误情况与测试向量

解析器遇到以下任何情况都必须报错，禁止容错修复。

| 代码 | 错误 |
| --- | --- |
| E01 | 带 BOM，或出现只含空白的行 |
| E02 | 到达输入末尾仍无结束符（截断） |
| E03 | 缩进不是恰好多 2 格，或缩进中含 tab |
| E04 | 条目行只有键，没有头 |
| E05 | 键名非法（含非法字符，或恰好是 `-` / `_`），或键与头之间不是恰好一个空格 |
| E06 | 同一层级键名重复 |
| E07 | 未知的头符号；或该位置出现了头而这里需要键（列表项出现在字典环境） |
| E08 | `_` / `{}` / `[]` 带同行载荷；或 `{}` / `[]` 下出现文本行 |
| E09 | `_` 下出现子层 |
| E10 | `-` 同时带同行载荷和子层；或它的子层里出现结构行 |
| E11 | 文本行出现在不允许的位置，或文本块结束后又出现文本行 |
| E12 | 顶层单独值之后又出现非注释行 |

**测试向量**（`↵` 表示 LF；期望值是“无 schema”下的解析结果）

| # | 输入 | 期望结果 |
| --- | --- | --- |
| 1 | `a - hello↵↵` | `{"a": "hello"}` |
| 2 | `a - x # y↵↵` | `{"a": "x # y"}` |
| 3 | `a - x  ↵↵` | `{"a": "x"}`，行尾空白去掉 |
| 4 | `a -↵  \|x  ↵↵` | `{"a": "x  "}`，文本行保留空白 |
| 5 | `a -↵  \|call - fake↵↵` | `{"a": "call - fake"}` |
| 6 | `a -↵  \|x↵  \|↵↵` | `{"a": "x\n"}` |
| 7 | `a -↵  \|↵  \|↵↵` | `{"a": "\n"}` |
| 8 | `a _↵↵` | `{"a": null}` |
| 9 | `a []↵  - s↵  - 2↵  _↵↵` | `{"a": ["s", "2", null]}` |
| 10 | `a {}↵↵` | `{"a": {}}` |
| 11 | `d - 2026-09-19↵↵` | `{"d": "2026-09-19"}`，是否读成日期属于 schema |
| 12 | `↵` | `{}` |
| 13 | `[]↵  - a↵  - b↵↵` | `["a", "b"]` |
| 14 | `-↵  \|x↵↵` | `"x"` |
| 15 | `# c↵- 3↵↵` | `"3"` |
| 16 | `- -↵↵` | `"-"`，第一个 token 是头，所以是顶层单独值 |
| 17 | `_↵↵` | `null` |
| 18 | `a -↵↵` | `{"a": ""}` |
| 19 | `a - 007↵↵` | `{"a": "007"}`，格式不做任何规范化 |
| 20 | `a {}↵  b - 1↵↵` | `{"a": {"b": "1"}}` |
| 21 | `a []↵  {}↵    b - 1↵↵` | `{"a": [{"b": "1"}]}` |
| 22 | `a - x` | E02 |
| 23 | `a↵↵` | E04 |
| 24 | `a % 007↵↵` | E07，`%` 不是头 |
| 25 | `a - x↵a - y↵↵` | E06 |
| 26 | `a -↵   \|x↵↵` | E03 |
| 27 | `a -↵  \|x↵  # c↵  \|y↵↵` | E11 |
| 28 | `- 3↵a - x↵↵` | E12 |
| 29 | `a {}↵  - - v↵↵` | E05，键不能恰好是 `-` |
| 30 | `a _↵  b - 1↵↵` | E09 |
| 31 | `a []↵  - x↵  b - 1↵↵` | E07 |
| 32 | `a -↵  \|# 不是注释↵↵` | `{"a": "# 不是注释"}` |
| 33 | `d @date 2026-09-19↵↵` | E07，`@` 不是头，也不是合法键 |

正式发布时，测试向量应另存为机器可读的文件，每条包含原始字节和期望值或错误代码。

## 12. 从 v0.1 到 v0.3 的变化

**v0.1 → v0.2**

- 删除标量类型符号 `~` `%` `/` `?`：它们不再是合法头，出现即 E07。
- `_` 从类型符号改成头，表示 null 值；null 在字典、列表、顶层三种位置都能显式写出。
- `-` 成为标量头。
- 行的种类由父头所处的环境决定：`{}` 的子层是条目行，`[]` 的子层是项行。
- 键名不能恰好是 `-` 或 `_`。
- 标量类型交给 schema；格式不再解析整数、浮点、布尔，v0.1 的 E09（数字/布尔字面量错误）随之退役。

**v0.2 → v0.3**

- 删除逃生口 `@name`。理由：所有标量本来就是字符串，类型的解释已经完全在 schema 侧，格式里再留类型标记是多余的。
- 数据模型明确为：字典、列表、字符串，外加 null（`_`）。
- 文本行只出现在 `-` 之下；头集合收敛为四个：`-`、`_`、`{}`、`[]`。
- E10 收窄为只针对 `-`；测试向量 11 改成普通字符串，并新增 `@` 被拒绝的用例。
- 相应地，JSON 映射里不再有 `{"$ext": ...}` 形态；number / bool 在无 schema 时就是字符串。

- 保留不变：`|` 块编码、空行结束符、2 空格缩进、`{}` / `[]` 结构、零转义与不可注入论证、流式与截断检测。

## 13. 待定问题

以下问题需要在 v1.0 之前决定。

- [ ] 正式名称。格式现在不做类型解释，`Line-Typed Format` 这个名字已经名不副实，是否改名（例如 Line-Structured Format）。
- [ ] MIME 类型和文件扩展名（暂定 `text/ltf`、`.ltf`）。
- [ ] 是否允许非 ASCII 键名，或提供一种能写任意键名（含 `-`、`_`、空格、`#`）的形式，以便完整映射 JSON。
- [ ] 是否规定嵌套深度和消息大小的上限，防止资源耗尽。
- [ ] 宽松模式（CRLF 转换）是否作为手写文件的默认模式。
- [ ] 顶层单独值是否必要，或能否统一用隐式键值对。
- [ ] 无 schema 时如何在需要时恢复 JSON 的 number / bool 区别（当前是明确放弃）。
- [ ] 是否提供一份参考 schema 语言，或只定义格式、schema 交给外部。
- [ ] README 中说明与 NestedText 的关系：两者都用行前缀免除转义，都把标量类型交给应用层；LTF 结构更显式，并增加流式与截断检测。
